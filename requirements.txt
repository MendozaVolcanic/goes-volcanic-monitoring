# GOES Volcanic Monitoring Dashboard
# Core data access
s3fs>=2024.0.0
xarray>=2024.0.0
h5py>=3.10.0
h5netcdf>=1.3.0
netcdf4>=1.6.0
numpy>=1.26.0

# Visualization & mapping
matplotlib>=3.8.0
plotly>=5.18.0

# Dashboard
streamlit==1.56.0           # PIN EXACTO (no rango) — critico para el frontend:
                            # cada version de Streamlit sirve sus chunks JS con
                            # OTRO hash (Checkbox.<hash>.js, Button.<hash>.js). Con
                            # un rango, cada rebuild de HF/uv podia instalar otro
                            # patch -> las pestañas abiertas fallaban al lazy-load
                            # un widget: "Failed to fetch dynamically imported
                            # module" (404 del chunk viejo). Pineado, los redeploys
                            # NO cambian los chunks -> las pestañas sobreviven.
                            # 1.56 da @st.fragment(run_every) + st.rerun(scope) y
                            # aun trae st.components.v1.html (removido recien >=1.60,
                            # lo usamos en live_viewer). Subir solo tras migrar eso
                            # y re-testear el frontend. (fix jun 2026, audit Bug B)

# Processing
scipy>=1.12.0
pyproj>=3.6.0

# API clients
requests>=2.31.0

# Utilities
pandas>=2.1.0
Pillow>=10.0.0

# YAML — lo usa tests/test_workflow_concurrency.py para parsear
# .github/workflows/*.yml (regla "un tag, un grupo de concurrency"). Sin el
# paquete, ese test se auto-saltea con pytest.importorskip("yaml") y CI
# reportaba verde con 9 tests MENOS que local. (fix audit ago-2026)
PyYAML>=6.0

# Video export (MP4 H.264) — imageio-ffmpeg trae binario ffmpeg incluido
# asi no requiere ffmpeg de sistema en Streamlit Cloud.
imageio>=2.34.0
imageio-ffmpeg>=0.4.9

# GeoTIFF export (preserva georeferencia para QGIS/GIS)
rasterio>=1.3.0
