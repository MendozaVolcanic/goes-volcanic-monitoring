# Auditoría GOES — Hallazgos (multi-agente, verificados)

**114 hallazgos** · 3 críticos · 16 altos · 35 medios · 60 bajos


## 🔴 Bugs (21)

- **[critical]** `dashboard/views/live_viewer.py:674` — _live_content perdió su @st.fragment — la vista 'En Vivo' ya no auto-refresca
  - `def _live_content()` (línea 674) NO tiene decorador, pero el docstring (línea 675), el footer y el contador JS prometen auto-refresh cada 60s. El único `@st.fragment(run_every="60s")` (línea 564) ahora decora a `_health_banner`, no al contenido. Verificado por git: antes del commit 9b9cebc el decorador envolvía a `_live_content`; el commit insertó `_health_banner` con su propio fragment y dejó `_live_content` sin decorar. Consecuencia: mapas, hot spots, viento, scans y descargas solo se refrescan en rerun manual; solo el banner tickea. Peor, el contador 'Próximo chequeo en X s' cuenta a 0 y nada ocurre, engañando al operador de guardia, que puede estar mirando un scan de 40+ min creyendo que es el último. Crítico para un dashboard NRT.
  - **Fix:** Restaurar `@st.fragment(run_every="60s")` sobre `def _live_content():`. Mantener selectores/toggles FUERA del fragment (gotcha CLAUDE.md); evaluar fragmentar solo el bloque de mapas para no resetear toggles en cada tick.

- **[critical]** `src/fetch/volcat_api.py:142, 146, 147` — _query_frames devuelve lista desnuda en error pero tupla (frames, coords) en éxito → ValueError al desempacar
  - El path de éxito retorna 2-tupla `return d.get('endtime') or [], d.get('coordinates')` (línea 147), pero los dos paths de error retornan `[]` (línea 142 status!=200, línea 146 excepción). Los tres callers desempacan con `frames, coords = _query_frames(...)` (volcat_latest L172/L180, volcat_at_time L238/L240). Ante cualquier hipo de SSEC (503/timeout/JSON inválido), `frames, coords = []` lanza `ValueError: not enough values to unpack`, anulando el manejo de error del try/except y propagando una excepción en vez de degradar a None. En la página VOLCAT principal (_render_height_section sin try/except) esto convierte el warning amistoso 'meta is None' en una página de excepción Streamlit; zonas_fullscreen.py:325 lo enmascara por casualidad con su try/except. También rompe el frame VOLCAT en el productor del Modo Sala y en build_backfill.
  - **Fix:** Hacer coherentes los returns: cambiar ambos `return []` por `return [], None` (líneas 142 y 146). Corregir la anotación a `-> tuple[list, dict | None]` y el docstring. Restaura el path 'meta is None' graceful.

- **[critical]** `dashboard/views/rammb_viewer.py:543` — NameError: scope_id_nacional() llamado pero nunca importado — la cobertura 'Nacional (Chile)' siempre crashea
  - En `_fetch_chile_frames` (línea 543) se llama `sid = scope_id_nacional()`, pero esa función NO está importada: los imports lazy de animation_cache traen solo {fetch_cached_frames, fetch_manifest} (línea 491) y {scope_id_volcan, scope_id_zona} (línea 577). `_fetch_chile_frames` se invoca incondicionalmente al cargar la cobertura 'Nacional (Chile)' (default, index=0), así que la animación nacional levanta `NameError: name 'scope_id_nacional' is not defined` SIEMPRE, capturado por el try/except de render() que muestra 'Error descargando: name scope_id_nacional is not defined'. El sibling _scope_id_from_bounds_key sí hace el lazy-import de las otras dos pero a esta función se le olvidó. Introducido el 2026-05-01 (commit fc67b6a).
  - **Fix:** Agregar el lazy-import dentro de _fetch_chile_frames, igual patrón que las otras funciones: `from src.fetch.animation_cache import scope_id_nacional` justo antes de `sid = scope_id_nacional()`.

- **[high]** `src/config.py:145-153` — Bbox de zona 'austral' no contiene ningún volcán austral; panel TV vacío y 'sur' saturado
  - VOLCANIC_ZONES['austral'] cubre lat[-56,-46], pero los 13 volcanes con zone='austral' del catálogo están entre lat -41.1 (Osorno) y -45.9 (Hudson) — dentro del bbox 'sur' (lat[-46,-36]). Las vistas de zona (zonas_fullscreen.py 128-131/408-410, live_viewer.py 129, volcat/ash viewer) filtran por BBOX, no por el campo .zone. Resultado: panel 'austral' = 0 volcanes (Patagonia/Magallanes sin volcanes RNVV), 'sur' = 30 volcanes amontonados e ilegibles. En el Modo Sala TV reescrito esto produce un panel austral permanentemente vacío y uno sur saturado. La partición OVDAS real ubica la austral en ~-41 a -46, no en -46 a -56. Los lon australes (-76..-71) también están corridos al oeste respecto de los volcanes (lon -72.0..-73.2).
  - **Fix:** Corregir límites: 'sur' = lat[-41,-36], 'austral' = lat[-46,-41]. Idealmente derivar los bboxes desde get_bounds(get_by_zone(zk), padding) en vez de hardcodear, y agregar un test que verifique que cada bbox contiene a todos sus volcanes de catálogo. Revisar también lon_min/lon_max australes.

- **[high]** `src/fetch/goes_s3.py:60-86` — download_band selecciona el último archivo de la hora, no el más cercano a dt — descorrelaciona bandas y timestamp
  - download_band lista todos los archivos L1b de la hora de `dt` y toma `files[-1]` (el scan más reciente de esa hora), ignorando el minuto solicitado. El pipeline Ash RGB (pipeline.py:66-72) llama download_band(dt, b) por separado para bandas 11/13/14/15. Como cada llamada elige independientemente el último scan, si aparece un scan nuevo en S3 entre descargas (cadencia 10 min, el pipeline tarda segundos), unas bandas vienen del scan T y otras de T+10min → el Ash RGB (B15-B14, B14-B11) y el BTD split-window restan bandas de scans distintos → artefactos de borde de nube y BTD espurio (falsos positivos de ceniza). Además pedir dt=14:00 puede devolver el scan de las 14:50 sin aviso, descorrelacionando la imagen del timestamp mostrado. Relacionado: si la hora está vacía cae a dt-1h y toma files[-1], pudiendo devolver una imagen de hasta ~2h antes sin señal al llamador.
  - **Fix:** Elegir el archivo cuyo start-time parseado (patrón `_sYYYYDDDHHMMSS`, como goes_fdcf._parse_scan_time) esté más cerca de `dt`, y devolver el scan_dt real (o loguear WARNING si difiere >15 min). Para el pipeline multi-banda, resolver UNA sola vez el scan objetivo y descargar todas las bandas de ese mismo scan.

- **[high]** `dashboard/views/replay_reciente.py:56` — Opción 'Láscar' del replay nunca resuelve (acento no matchea el catálogo)
  - ACTIVE_VOLCANOES define "Láscar" (con tilde) pero el CATALOG lo registra como "Lascar" (sin tilde, limpieza OVDAS oficial). get_volcano hace match por substring case-insensitive (`name_lower in v.name.lower()`): `"láscar" in "lascar"` es False → get_volcano("Láscar") devuelve None → render() entra en el branch `if v is None`, muestra st.error('Volcán Láscar no encontrado') y hace return. La opción está rota al 100% para uno de los pocos volcanes chilenos con hot spots térmicos. El docstring y el subtítulo ya escriben 'Lascar' sin tilde, así que el bug es solo la entrada del dict.
  - **Fix:** Cambiar la línea 56 a `{"name": "Lascar", ...}` para coincidir con el nombre canónico del catálogo (igual que ya hacen docstring y header).

- **[high]** `dashboard/views/replay_reciente.py:219` — Replay cae a un frame near-live cuando el timestamp histórico falla, mal etiquetado como el pasado
  - El fallback pasa `[selected_ts] + timestamps[:5]` a fetch_frame_robust, que prueba los ts en orden y devuelve el primero que cargue. Si el frame histórico elegido (de hasta ~28 días atrás) no carga en RAMMB, la función no prueba ts cercanos sino los 5 MÁS RECIENTES (timestamps[:5] = newest-first), mostrando una imagen casi en vivo mientras el label sigue diciendo la fecha histórica pedida (solo agrega ' ⚠ ts cercano', texto que sugiere 'cercano en el tiempo' cuando es 'hoy'). Para una herramienta de replay cuyo propósito es revisar un momento pasado, mostrar el presente disfrazado del pasado es un error de integridad que puede inducir conclusiones falsas sobre un evento.
  - **Fix:** Construir el fallback con ts adyacentes al seleccionado: `neighbor = timestamps_chrono[max(0,idx-2):idx+3]`; pasar `[selected_ts] + [t for t in neighbor if t != selected_ts]`. Si used_ts != selected_ts, calcular el gap real en minutos y mostrarlo (o bloquear el frame si supera ~30 min).

- **[high]** `dashboard/app.py:92-93` — Salir fullscreen no escapa los valores de query params (rompe permalinks con espacios/acentos)
  - El botón 'Salir fullscreen' reconstruye la querystring con `_qs = "&".join(f"{k}={v}" ...)` sobre valores crudos. Varios params contienen caracteres que requieren URL-encoding: `volcan=Nevados de Chillán`, `volcan=Planchón-Peteroa` (espacios, acentos), o un timestamp. Sin codificar, el href produce una URL inválida: el espacio corta el atributo href y el navegador descarta lo que sigue. Resultado: al salir de fullscreen desde Modo Evento/Guardia con un volcán de nombre compuesto, se pierde el volcán seleccionado o la navegación queda rota. render_top_navigation_button (map_helpers.py:168) tiene el mismo patrón sin escape, y modo_guardia.py:392/408-411 le inyectan el nombre del volcán crudo, así que el helper es el punto natural para arreglarlo de raíz.
  - **Fix:** Usar urllib.parse.quote sobre cada valor: `"&".join(f"{k}={urllib.parse.quote(str(v))}" ...)`. Centralizar el escape dentro de render_top_navigation_button (construir la query con urlencode) para que todos los callers queden seguros.

- **[high]** `src/process/geo.py:88-109` — crop_to_bounds devuelve el casco rectangular sin re-enmascarar — fuga de píxeles fuera del bbox
  - crop_to_bounds construye una máscara booleana correcta pero devuelve el casco rectangular [r_min:r_max+1, c_min:c_max+1] SIN re-aplicar la máscara. Si el bbox intersecta píxeles en filas/columnas no alineadas (lat varía por filas, lon por columnas — el caso real de get_lat_lon), el slice rectangular incluye píxeles cuyo lat/lon caen FUERA del bbox: se filtran de contrabando. Los KPI por volcán y los recortes por zona se calculan sobre estos arrays y heredarían señal de áreas vecinas. El test actual usa una grilla eje-alineada donde el casco colapsa a 1 píxel, así que nunca ejercita la fuga (verificado empíricamente: bbox que toca 2 esquinas opuestas devuelve casco 3×3 con 7 de 9 píxeles fuera del bbox).
  - **Fix:** Re-enmascarar antes de devolver (poner NaN o filtrar los píxeles del casco cuyo lat/lon no cumplen el bbox), y agregar un test test_crop_to_bounds_rectangular_hull con lat por filas y lon por columnas que assertee que todos los píxeles devueltos cumplen el bbox.

- **[medium]** `dashboard/views/live_viewer.py:74, 126, 137` — Cache-buster `_v=_REPROJECT_VERSION` es inerte: el prefijo '_' lo excluye de la clave de @st.cache_data
  - _fetch_frame_for_ts (l.74), _fetch_zone_frame (l.126) y _fetch_volcano_frame (l.137) reciben `_v: str = _REPROJECT_VERSION` con la intención (líneas 53-54) de bustear la cache al cambiar la versión de reproyección. Pero @st.cache_data IGNORA por diseño los parámetros con prefijo guión bajo (ese es el mecanismo documentado de opt-out de hashing). Por lo tanto `_v` no forma parte de la clave y bumpear _REPROJECT_VERSION de 'v2' a 'v3' NO invalida nada: se sirven frames con la matemática vieja hasta que expire el TTL (2h) o se reinicie el proceso. Si se ajusta cfac/bounds y se bumpea la versión esperando frames frescos, se ven imágenes mal georeferenciadas calladamente.
  - **Fix:** Renombrar `_v` a un nombre SIN guión bajo (`v` o `version`) en las tres funciones para que entre en la clave de hash. Alternativamente, concatenar _REPROJECT_VERSION dentro de un argumento ya hasheado (p.ej. `ts=f'{ts}|{_REPROJECT_VERSION}'`).

- **[medium]** `src/fetch/goes_fdcf.py:64-120` — Inconsistencia HIGH_CONF_MASK vs _confidence_from_mask: categorías 30/31 quedan con confidence='unknown'
  - HIGH_CONF_MASK = {10, 11, 30, 31} se usa como conjunto de alta confianza cuando high_conf_only=True; el docstring explica que 30/31 son 'processed temporally filtered' y por eso se incluyen. Pero _confidence_from_mask (113-120) solo mapea 10/11→'high', 12/13→'saturated', 14/15→'low' y cae a 'unknown' para todo lo demás. Resultado: con high_conf_only=True, los hotspots de categoría 30/31 se devuelven etiquetados confidence='unknown', contradiciendo que se los seleccionó por ser de alta confianza. Cualquier consumidor que filtre/coloree por confidence tratará esos píxeles válidos como dudosos.
  - **Fix:** Agregar 30/31 al mapeo de _confidence_from_mask devolviendo 'high' (o 'high_filtered' si se quiere distinguir), para que la etiqueta quede coherente con el criterio de selección documentado.

- **[medium]** `dashboard/views/modo_guardia.py:326-330` — _rotating_chile_tv rota vía contador en session_state, reintroduciendo el bug 'trabado en slot 0 tras reconnect'
  - _rotating_chile_tv avanza la rotación con un contador in-memory `st.session_state[session_key]` incrementado cada tick. zonas_fullscreen.py:786-794 documenta que deliberadamente MOVIERON la rotación de 4 zonas a un índice por reloj (`int(now.timestamp() // TV_SLOT_SECONDS) % N`) precisamente porque el watchdog/reconnect limpia session_state, reseteando el contador a 0 y dejando la rotación 'trabada' (slot 0). El path `?tv=chile` sigue usando el patrón viejo de session_state y tiene exactamente el bug ya arreglado para `?tv=1`: tras un reconnect en una pantalla 24/7, la rotación Chile reinicia en GeoColor/index 0 en vez de continuar por reloj.
  - **Fix:** Reemplazar el contador por un índice por reloj, igual que _rotating_tv_zonas: `idx = int(datetime.now(timezone.utc).timestamp() // ROTATION_SECONDS) % len(CHILE_PRODUCT_LIST)`, descartando el read/write de session_state y el argumento session_key.

- **[medium]** `dashboard/views/modo_evento.py:450-451` — Escritura de query_param 'volcan' en cada render compite con el router de slugs (navegación frágil)
  - render() escribe `st.query_params['volcan'] = volcan` en cada pasada. El CLAUDE.md advierte que routing/permalinks se manejan vía PAGE_SLUGS en app.py y que la navegación por query_params es frágil en el iframe de Streamlit Cloud. El selectbox está fuera del fragment (bien), pero la sincronización de URL corre en cada rerun completo: si el usuario cambia de vista por sidebar mientras esta página está activa, la reescritura de `?volcan=` puede competir con la escritura del slug `?vista=` y dejar la URL inconsistente. Relacionado: app.py preserva 'volcan' al cambiar de vista (comentario línea 201), así que el param viaja a Modo Guardia TV (modo_guardia.py:742/752) que lo adopta como volcán activo aunque el usuario nunca lo eligió allí.
  - **Fix:** Condicionar la escritura a que la vista activa sea realmente 'evento' antes de tocar `?volcan=`, y considerar moverla a un on_change callback del selectbox. Al cambiar de vista, limpiar 'volcan' de query_params cuando el slug destino no lo usa, o namespacear los params por vista.

- **[medium]** `dashboard/app.py:65` — El branch 'Salir fullscreen' no reconoce tv=chile/tv=zonas — dos botones de salir apilados
  - La guardia `_tv_active = st.query_params.get('tv','') in ('1','mosaico','volcan')` enumera los modos TV para suprimir el botón genérico 'Salir fullscreen' cuando ya hay un '✖ Salir' propio del modo TV. Pero modo_guardia.py:392 introduce `tv=chile` y el docstring documenta `tv=zonas` como alias de `tv=1`; ninguno está en la tupla. Consecuencia: al entrar a Modo Sala · Chile (tv=chile), _tv_active es False y se renderiza el botón genérico ADEMÁS del '✖ Salir' del modo TV — exactamente el caso de 'dos botones apilados' que el comentario dice querer evitar.
  - **Fix:** Incluir todos los valores TV: `in ('1','zonas','chile','mosaico','volcan')`, o invertir a `_tv_active = bool(st.query_params.get('tv'))` ya que cualquier valor no vacío implica modo TV con su propio botón de salir.

- **[medium]** `dashboard/app.py:157-160` — El redirect de slugs viejos no fuerza nav_page en el mismo rerun (radio queda en página equivocada)
  - Al entrar con un slug redirigido (`?vista=zonas` → 'guardia'), el bloque escribe `st.query_params['vista']='guardia'` y re-lee qp. En sesión nueva funciona. El caso que falla: si `__last_url_vista` ya valía 'guardia' de una visita previa y el usuario vuelve con el slug viejo `zonas`, tras el redirect `_url_vista='guardia' == _last_url_vista` → el sync NO corre y nav_page conserva el valor de session_state previo (que puede no ser Guardia). El redirect debería forzar la adopción del destino.
  - **Fix:** Tras el redirect, setear explícitamente `st.session_state['nav_page'] = PAGE_SLUGS[_SLUG_REDIRECTS[_curr]]` y `st.session_state['__last_url_vista'] = _SLUG_REDIRECTS[_curr]`, de modo que el destino siempre gane.

- **[low]** `dashboard/views/ash_viewer.py:184` — _compute_insight: rama 'ok'/'Sin actividad significativa' contradictoria con N píxeles BTD<-1K detectados
  - La rama `if ash_px > 0: return f'Sin actividad significativa - {ash_px} pixeles con BTD < -1K', 'ok'` (l.184) etiqueta como 'ok'/'sin actividad' cualquier caso con 1..100 píxeles de BTD negativo. El banner resultante es contradictorio. En Chile invierno cirros/nieve generan decenas de píxeles BTD<-1K (el CLAUDE.md advierte 30-60% de falsos positivos), así que esta rama se dispara seguido con mensaje confuso. El umbral físico -1K es correcto (Prata 1989); el problema es la redacción/semáforo.
  - **Fix:** Reformular para coherencia con 'ok', p.ej. f'Señal débil/ruido - {ash_px} px BTD<-1K (probable cirro/nieve)', 'ok'. O subir el umbral inferior para no resaltar conteos triviales. Mantener el threshold físico.

- **[low]** `dashboard/views/comparador.py:274-283` — Modo Antes/Después permite idx_a > idx_b → 'intervalo' negativo confuso en el caption
  - En _mode_antes_despues ambos selectbox son libres sobre el mismo eje cronológico, así que el usuario puede elegir ANTES más reciente que DESPUÉS. delta_min = (ts_b - ts_a) puede ser negativo y el caption imprime 'intervalo -120 min' (con la división/módulo de Python para negativos dando textos sin sentido como '-3h 50min'). No rompe nada pero produce un texto sin sentido para el operador.
  - **Fix:** Usar abs(delta_min) para el display y/o ordenar (ts_a, ts_b) por tiempo antes de calcular: `dm = abs(delta_min); f'intervalo {dm} min ({dm//60}h {dm%60}min)'`.

- **[low]** `dashboard/views/loop_volcan.py:166-171` — 'loop=True' no es argumento válido de la config de animación Plotly (ignorado; no hay loop infinito real)
  - El botón ▶ Play incluye `loop=True` dentro de los kwargs de animación de Plotly.js, que no reconoce esa clave (los campos válidos son frame, transition, mode, fromcurrent, direction). El parámetro se ignora, así que la animación corre una vez del frame actual al final y se detiene. El docstring y la caption prometen 'auto-loop continuo' que no ocurre con un solo click.
  - **Fix:** Plotly no soporta loop nativo vía updatemenus. Quitar la clave inválida loop=True y ajustar la caption a 'corre una pasada · re-presioná ▶ para repetir', o implementar el re-trigger JS sobre plotly_animated si el loop infinito es requisito.

- **[low]** `dashboard/views/volcat_viewer.py:114` — _fig_ssec_image usa scaleratio=1, estirando la imagen SSEC ~20% en latitudes de Chile
  - _fig_ssec_image setea `yaxis=dict(scaleanchor='x', scaleratio=1)`. Per el gotcha del CLAUDE.md y el resto del código, los plots geográficos usan scaleratio = 1/cos(lat) (modo_guardia_volcan.py:358, mosaico_chile.py:164, zonas_fullscreen.py:177/456, comparador.py:129). Con scaleratio=1 y sizing='stretch', el Full-Disk SSEC se muestra con unidades lon y lat iguales, estirándose ~20% vertical en Chile central (~-35°, cos≈0.82). Los markers de volcán siguen correctos (comparten ejes), así que es distorsión de aspecto, no error de georef. Impacto limitado: solo alimenta el _render_ssec_rgb_block redundante (expander colapsado).
  - **Fix:** Calcular `cos_lat = max(0.1, cos(radians((lat_min+lat_max)/2)))` desde bounds y setear `scaleratio=1.0/cos_lat`, igual que las demás figuras geográficas.

- **[low]** `scripts/build_frp_timeline.py:136, 189` — Poda de ventana usa 'now' sin alinear a step → la ventana oscila ±1 scan entre corridas
  - El cutoff de la ventana rodante usa `now` (minutos sin alinear a step): `cutoff = (now - timedelta(hours=window_hours)).strftime(ISO)`. Los scans guardados tienen claves alineadas a step (_round_to_step). Como now rara vez coincide con un múltiplo de step, el borde de la ventana cae 'entre' slots y puede descartar/conservar un scan de más. Efecto: la ventana oscila ±1 scan entre corridas (no-determinista, no corrompe datos).
  - **Fix:** Usar `now_floor` (ya alineado a step) para el cutoff de la ventana de scans. El roll-up diario corta por día, así que ahí da igual.

- **[low]** `dashboard/views/live_viewer.py:88` — _fetch_frame_for_ts reproyecta Nacional con col_start/row_start hardcodeados (678/1356) en vez de derivar del tile_sz
  - _fetch_frame_for_ts llama reproject_to_latlon con col_start=678, row_start=1356 HARDCODEADOS, mientras fetch_frame_for_bounds (rammb_slider.py:449-451) los deriva del tile_sz real. El offset 678 asume tile_sz=678 (zoom 2); si RAMMB devolviera 512 para geocolor a zoom 2, la reproyección quedaría desalineada (correctness latente). El reproyectado ya está bien cacheado por ts, así que el costo es acotado; el riesgo es de alineación si cambia el tile_sz.
  - **Fix:** Derivar col_start/row_start del tile_sz real del canvas como hace fetch_frame_for_bounds, en vez de hardcodear 678/1356.


## 🧵 Concurrencia (9)

- **[high]** `src/fetch/goes_s3.py:60-86` — download_band escribe RAW_DIR/filename sin lock ni escritura atómica — corrupción con descargas paralelas
  - download_band/download_mcmip/download_fdc hacen `fs.get(remote_path, str(local_path))` directo sobre el path final (s3fs escribe in-place, no tmp+rename) y luego `use_cache and local_path.exists()` lo da por válido. Dos problemas combinados: (1) Concurrencia: download_band corre bajo ThreadPoolExecutor(max_workers=4) en hires_pipeline._download_bands_parallel y desde live_viewer/loop_volcan/rammb_viewer; dos threads que piden la misma banda/scan calculan el mismo filename y hacen fs.get() simultáneo sobre el mismo path → NetCDF entrelazado/truncado → open_band revienta con error HDF5 o, peor, lee BT corruptas (ceniza/altura falsa). Es exactamente la raza que el código ya blindó en historic_l1b_rgb.py:58-72 con _DL_LOCKS, pero ese fix vive solo en el wrapper, no en la función canónica. (2) Atomicidad: un fetch interrumpido (timeout, OOM en HF cpu-basic, kill) deja un .nc parcial que el cache sirve como válido para siempre hasta borrar RAW_DIR a mano. hires_pipeline._download_bands_parallel es el call-site que vuelve la raza concreta y frecuente (GeoColor del zoom Villarrica del TV pasa por aquí).
  - **Fix:** Arreglar la raíz en goes_s3: descargar a un tmp único (`local_path.with_suffix('.part.<uuid>')`) y `os.replace()` atómico al destino solo si fs.get terminó sin excepción; combinar con un dict de threading.Lock keyed por filename (re-chequear exists() bajo lock). Factorizar la descarga atómica en una helper que use también historic_l1b_rgb, eliminando la doble implementación. Aplicar a download_mcmip y download_fdc.

- **[high]** `dashboard/views/zonas_fullscreen.py:879` — Arranque del productor (_TV_PRODUCER_STARTED) es check-then-set no atómico — pueden quedar 2+ hilos productores
  - prewarm_tv_caches() hace 'if _TV_PRODUCER_STARTED: return' y después '_TV_PRODUCER_STARTED = True' (líneas 925-928) sin lock. Se invoca desde modo_guardia.py:879 en cada render del modo TV. Streamlit corre el script de cada sesión en su propio thread; con 2 pestañas/usuarios abriendo el Modo Sala casi a la vez (o primera carga + reconnect del watchdog), dos threads pueden leer False antes de que cualquiera ponga True → se lanzan DOS o más hilos 'tv-producer'. Cada uno corre _produce_once cada 20s: descargas RAMMB/SSEC duplicadas, doble presión sobre s3fs/_DL_LOCKS, composiciones PIL duplicadas reteniendo el GIL — justo lo que TV_PRODUCER_IDLE_S buscaba evitar. Los hilos son daemon y nunca se joinean, el leak persiste toda la vida del proceso.
  - **Fix:** Proteger el gate con un threading.Lock module-level: 'with _PRODUCER_START_LOCK: if _TV_PRODUCER_STARTED: return; _TV_PRODUCER_STARTED = True' y arrancar el hilo dentro/justo después del lock. Spawn exactamente-una-vez por proceso.

- **[high]** `dashboard/views/zonas_fullscreen.py:998-1007, 933-968` — Hilo productor llama @st.cache_data sin ScriptRunContext garantizado — cache no se calienta (sala con placeholders)
  - _producer() (hilo daemon) llama dentro de _produce_once funciones decoradas @st.cache_data: _recent_ts (l.933), _compose_4_zonas_png, _volcat_latest_cached/_volcat_map_only/_volcat_colorbar_strip/_volcat_image_with_overlays (l.954-968), _volcan_zoom_png — que además abren ThreadPools cuyos workers no heredan contexto. El intento de adjuntar ctx (add_script_run_ctx(t), l.1000-1001) está en un try/except que SE TRAGA el fallo: si el import de scriptrunner cambia de ruta (ha pasado entre versiones) el hilo arranca sin contexto y cada llamada cacheada emite 'missing ScriptRunContext' y, según versión, devuelve sin cachear (el productor 'produce' pero nada queda caliente → el foreground siempre ve placeholder, el síntoma que el productor pretendía resolver) o lanza NoSessionContext. Además el ctx se copia del thread de la PRIMERA sesión que renderizó el TV; al cerrarse esa sesión, las escrituras de cache se atribuyen a una sesión muerta. El gasto se repite cada 20s mientras la sala esté activa.
  - **Fix:** Lo robusto: mover el cómputo pesado del productor a funciones PURAS no decoradas con st.cache_data, cacheando a mano en dicts module-level con lock+TTL, evitando la dependencia del ScriptRunContext desde el hilo. Si se mantiene st.cache_data, propagar add_script_run_ctx a los workers de los ThreadPools internos (initializer del executor) y NO tragar el fallo de add_script_run_ctx (loguearlo) para detectar en deploy si el cache realmente se calienta.

- **[medium]** `src/fetch/animation_cache.py:134, 143, 150` — _zip_cache (dict de módulo) sin lock ni cota: TOCTOU/doble-descarga y crecimiento ilimitado
  - _zip_cache (scope,product)→(ts,bytes) se lee (l.143) y escribe (l.150) sin lock; la secuencia check-then-act no es atómica, así que dos reruns con el mismo (scope,product) en miss/expirado bajan el ZIP (~3-7 MB) ambos en paralelo (duplica ancho de banda justo en el path que el cache busca optimizar). Además el cache no tiene cota ni evicta: con ~42 keys (5 zonas + nacional + 8 volcanes × 3 productos) puede retener decenas de MB indefinidamente; los entries expirados por TTL siguen en RAM hasta que se vuelven a pedir.
  - **Fix:** Envolver el check-then-act en un threading.Lock de módulo y purgar entradas con `now - ts >= _ZIP_TTL` al insertar. Si el caller corre bajo @st.cache_data, considerar mover este cache a st.cache_data y eliminar el dict manual.

- **[medium]** `src/fetch/rammb_slider.py:203, 375` — ThreadPools anidados 4×8 contra RAMMB sobre una pool HTTP de 16 conexiones (keep-alive saturado)
  - En el grid de 4 zonas, _compose_4_zonas_png lanza un ThreadPool de 4 zonas y cada zona dispara fetch_stitched_frame con su propio ThreadPool de 8 workers (rammb_slider.py:375) → hasta 32 descargas de tiles concurrentes contra el mismo host RAMMB, todas sobre la Session singleton con pool_maxsize=16 (_http_session.py:49). Cuando se exceden las 16, urllib3 bloquea/serializa o crea conexiones descartables, perdiendo keep-alive y pudiendo disparar throttling/timeouts de RAMMB (documentado como intermitente).
  - **Fix:** Subir pool_maxsize de la Session a ≥ el máximo concurrente real (p.ej. 32), o reducir el anidamiento bajando los workers internos de fetch_stitched_frame (p.ej. 4) cuando se invoca dentro del fan-out de 4 zonas, acotando el total concurrente a ~16.

- **[medium]** `dashboard/views/zonas_fullscreen.py:775, 991` — Gate de ocio del productor usa time.time() (wall clock) en vez de time.monotonic()
  - El gate compara `_time.time() - _TV_LAST_SEEN < TV_PRODUCER_IDLE_S` (l.991, hilo productor) mientras el foreground escribe `_TV_LAST_SEEN = _time.time()` (l.775). Usa wall clock: si el reloj del contenedor salta hacia atrás (NTP step, común en VMs/containers), el productor puede quedar ocioso indefinidamente aunque la sala esté activa (síntoma: 'la sala TV se congela hasta refrescar'), o componer aunque nadie mire. El valor solo se usa como delta, así que monotonic es estrictamente mejor. Relacionado: _TV_LAST_SEEN no se setea antes del primer prewarm, así que tras un restart el productor arranca ocioso ~20s extra aunque haya espectador desde el segundo 0.
  - **Fix:** Usar time.monotonic() tanto en la escritura (l.775) como en la comparación (l.991). Además, setear _TV_LAST_SEEN = time.monotonic() dentro de prewarm_tv_caches() justo antes de lanzar el hilo, para que la primera iteración componga de inmediato.

- **[medium]** `dashboard/views/zonas_fullscreen.py:915-937` — Productor siembra show_hotspots/volcan_zooms de la PRIMERA sesión y los ignora después (estado global cross-sesión)
  - prewarm_tv_caches() está protegido por _TV_PRODUCER_STARTED: solo el primer llamado crea el hilo, capturando show_hotspots y TV_VOLCAN_ZOOMS en el closure de _producer. En HF/Streamlit Cloud el proceso es compartido por todas las sesiones, y _TV_PRODUCED es un dict global. Si la primera sesión abre el TV con una config y otra lo abre con otra (o si se parametriza el toggle de hotspots por usuario, o se agregan volcanes a TV_VOLCAN_ZOOMS en runtime), la segunda lee PNGs producidos con los parámetros de la primera, silenciosamente. Hoy los params son fijos (show_hotspots=True hardcoded en modo_guardia:879) así que no diverge, pero es un acoplamiento frágil.
  - **Fix:** Documentar explícitamente que el productor es de proceso-global y sus parámetros son fijos (no por-sesión). Si en el futuro se parametriza el TV por usuario, keyear _TV_PRODUCED por la config (o leer los flags desde una variable de módulo que el foreground actualice cada run) en vez de capturarlos en el closure.

- **[medium]** `src/fetch/_http_session.py:36-52` — Singletons lazy-init no atómicos (_get_fs / get_session): 2 instancias y pérdida de retry en la ráfaga de arranque
  - _get_fs() (goes_s3.py:28-33) y get_session() (_http_session.py:36-52) hacen 'if X is None: X = construir()' sin lock. Ambos se llaman desde múltiples ThreadPoolExecutors (hasta 12 workers en wind_data, 8 en rammb_slider, 4 en hires). En la primera ráfaga concurrente, dos threads pueden ver None y construir dos instancias; el perdedor queda huérfano. Para s3fs, construir dos en paralelo puede disparar warnings de event loop o usar una instancia a medio-inicializar. Para requests.Session, el perdedor puede emitir 1-2 requests con una Session sin el HTTPAdapter/Retry montado todavía, perdiendo los reintentos que el módulo promete.
  - **Fix:** Double-checked locking con un threading.Lock module-level en ambos: 'if X is None: with _LOCK: if X is None: X = construir()'.

- **[low]** `src/fetch/_http_session.py vs múltiples vistas:n/a (varios módulos)` — _volcat_uniform_views / _uniform_aspect_bounds / _GEO_CACHE: caches lazy module-level sin lock (benigno, idempotente)
  - _volcat_uniform_views, _uniform_aspect_bounds (zonas_fullscreen.py 265/1097) y _GEO_CACHE (map_helpers.py 37-49) usan el patrón check-then-set con globals sin lock, invocados desde el productor y el foreground. Una raza de inicialización solo significa que dos threads computan el mismo dict determinístico (función pura de constantes) y uno pisa al otro — resultado idéntico, asignación atómica bajo GIL, sin corrupción. Se registra para que un futuro auditor no lo 'arregle' con overhead innecesario.
  - **Fix:** No requiere fix funcional (cómputo idempotente). Si se quiere prolijidad, usar functools.lru_cache sobre una función sin args (thread-safe) o pre-computar al import. Dejar nota de que es deliberadamente sin-lock por idempotencia.


## ⚡ Eficiencia (13)

- **[high]** `src/fetch/goes_fdcf.py:204-217, 334-341` — FDCF lee el NetCDF full-disk (Mask/Power/Temp/Area 5424x5424, ~470 MB) en RAM por cada consulta de hotspots por zona
  - fetch_latest_hotspots y fetch_hotspots_at_time hacen `.values` de Mask (uint8 ~29 MB) y Power/Temp/Area (float32 ~118 MB c/u) sobre el full-disk completo ANTES de filtrar por bbox → ~380-470 MB de pico por scan. En el grid de 4 zonas, _hotspots_zone se cachea por zona pero cada una dispara una lectura full-disk distinta del MISMO scan → 4 lecturas completas del mismo .nc por refresco. En HF cpu-basic (RAM limitada) es el pico de memoria + I/O S3 más caro del data chain y puede gatillar OOM. El repo ya tiene la solución en frp_timeline.fetch_scan_sliced (recorta el sub-bloque ABI a Chile vía _chile_xy_index_range), pero los paths NRT y backfill siguen leyendo full-disk aunque casi siempre reciben bounds de Chile.
  - **Fix:** Unificar las tres implementaciones: cachear UNA lectura del scan completo de Chile (ttl 300) y derivar las 4 zonas filtrando in-memory, o recortar a los índices x/y del bbox con _chile_xy_index_range antes de materializar Power/Temp/Area (igual que fetch_scan_sliced) en vez de traer 380 MB.

- **[high]** `src/process/hires_pipeline.py:231-235` — mode='color' materializa reflectancia y lat/lon full-disk (banda 2 ~1.9 GB) — riesgo OOM en runner 7 GB
  - En mode='color', `_downsample_2x(refls[2])` opera sobre la reflectancia full-disk de banda 2 (21696x21696 a 0.5km ≈ 1.9 GB float32) y `get_lat_lon(datasets[ref_band])` (banda 1km 10848x10848) recorre el camino de geo.get_lat_lon, que crea ~10 arreglos 2D float64 de 10848² (~0.94 GB c/u) → pico de varios GB. El modo 'mono_05km' ya implementa el atajo super-bbox 1D (_scope_pixel_bounds, isel antes de materializar, líneas 204-230) pero la rama 'color' (231-235) no lo aplica, pudiendo OOM-matar el contenedor cpu-basic / runner GH free, cuando los scopes son bboxes de ±0.5° que cubren una fracción mínima del disco. Mismo problema en historic_l1b_rgb.py:222 y, menos grave, pipeline.py:81 (banda 14 2km).
  - **Fix:** Aplicar a la rama 'color' la estrategia super-bbox de mono_05km: calcular pixel-bounds de todos los scopes, isel() de bandas 1/2/3/13 a esa sub-región ANTES de reflectancia/downsample/get_lat_lon. Alternativamente, float32 en geo.get_lat_lon y `del` de intermedios.

- **[medium]** `dashboard/views/live_viewer.py:1409-1416` — Viento por volcán sin cache: 27 requests a Open-Meteo por rerun (vs path Nacional/Zona cacheado)
  - En la tab Volcán, el viento llama `fetch_wind_grid` DIRECTAMENTE (importado como _fwg), sin pasar por _fetch_wind_cached (ttl=3600). fetch_wind_grid sobre una grilla 3×3 dispara 9 requests a Open-Meteo. Como st.tabs renderiza el cuerpo de las 3 sub-pestañas (GeoColor/Ash/SO2) en cada ejecución server-side, este bloque corre 3 veces por rerun = 27 requests cada vez que el usuario interactúa con el toggle de viento activo. Los paths Nacional (l.1068) y Zona (l.1250) sí reutilizan wind_data_cached vía _fetch_wind_cached. Latencia visible + riesgo de rate-limit de Open-Meteo + desperdicio (la grilla 3×3 solo cambia con volcán/radio, no con producto).
  - **Fix:** Envolver la grilla por-volcán en una función cacheada análoga: `@st.cache_data(ttl=3600) def _fetch_wind_volcano(lat, lon, radius, level): ...` y llamarla con WIND_LEVELS[wind_level], para que las 3 sub-tabs compartan el resultado.

- **[medium]** `dashboard/views/modo_guardia_volcan.py:580-584` — Captura PNG compuesta en cada tick de 60s aunque nunca se descargue
  - _live_panel es @st.fragment(run_every=60s). Con el toggle 'Captura' activo, _build_capture_png (composite PIL de un canvas 1800x1200 con 3 paneles LANCZOS, load_font, encode PNG) corre en CADA tick de 60s para tener el dato listo para st.download_button, descargue o no el usuario. En HF cpu-basic es un costo recurrente de CPU/GC por minuto para un artefacto usualmente nunca descargado. El mismo concern PIL-holds-GIL motivó el idle-gate del productor en zonas_fullscreen.
  - **Fix:** Diferir la composición hasta que se solicite: envolver _build_capture_png en @st.cache_data keyed por (volcan, scan timestamp) para que ticks idénticos reusen el resultado, o renderizar la descarga vía callback on-demand (build solo al click).

- **[medium]** `dashboard/views/zonas_fullscreen.py:888-904` — Payload base64 gigante reenviado en cada ForwardMsg del fragment TV (cada 12s) por WebSocket
  - _emit_tv_png inyecta el PNG completo como data URL base64 dentro de st.markdown. El PNG compuesto de 4 zonas (ancho ~1300-2000px) y el zoom de volcán son cientos de KB; base64 los infla +33%. Como _rotating_tv_zonas corre cada TV_SLOT_SECONDS=12s y re-emite el <img> inline, cada slot manda el payload completo por WebSocket (ForwardMsg) aunque la imagen no haya cambiado → varios MB/min sostenidos 24/7 en HF cpu-basic. El propio path ssec_overlay usa st.image(img) (l.558) que sirve por URL /media/ cacheable por hash, evitando re-enviar bytes.
  - **Fix:** Reemplazar el <img src=data:base64> de _emit_tv_png por st.image(png) (sirve vía /media/ con hash → el navegador cachea y no re-baja al repetir slot) y aplicar el centrado/escala 100vw-100vh por CSS sobre el contenedor stImage como ya se hace en ssec_overlay.

- **[medium]** `dashboard/views/zonas_fullscreen.py:931-983` — El productor re-compone las 4 zonas y re-pollea timestamps cada 20s aunque el scan RAMMB cambie cada ~10 min
  - _produce_once corre cada TV_PRODUCER_PERIOD_S=20s y llama _rgb_png(product) para los 3 productos. _compose_4_zonas_png está cacheada (ttl 900) y el fetch+reproyección se reutiliza, pero cada pasada vuelve a llamar _recent_ts (request a latest_times.json por los 3 productos), re-evalúa specs VOLCAT y baja/abre frames VOLCAT (ttl 300, no alineado a 20s). RAMMB publica un scan nuevo solo cada ~10 min, así que es trabajo repetido ~30 veces entre scans reales, compitiendo con el render del fragment en CPU compartida de HF.
  - **Fix:** Saltar el ciclo del productor si el set de timestamps RAMMB y el datetime VOLCAT no cambiaron desde la última pasada (guardar el último ts visto a nivel módulo, early-return de _produce_once cuando coinciden). Entre scans reales el productor queda casi ocioso sin perder frescura. Combinado, elimina ~30/30 composiciones PIL entre scans.

- **[medium]** `src/process/geo.py:38` — get_lat_lon materializa ~10 grids 2D float64 sin necesidad (memoria y velocidad)
  - np.meshgrid(x,y) y las operaciones siguientes (a, b, c, disc, r_s, s_x, s_y, s_z, lat, lon) son float64 2D del tamaño completo del slice. Para full-disk son ~0.94 GB/array y ~10 vivos simultáneamente. Aun sin OOM, el costo de tiempo y memoria es ~2x el necesario: la georef GOES-R es estable en float32 (<100 m). `valid = disc >= 0` y `r_s = np.full_like(disc, nan)` crean copias extra evitables. Es el motor común detrás del riesgo OOM del modo color de hires_pipeline.
  - **Fix:** Castear x/y a float32 tras meshgrid (o computar en float32 explícito), reutilizar buffers in-place donde se pueda y `del` de los intermedios tras calcular lat/lon. Opcionalmente aceptar un slice (r0,r1,c0,c1) para no materializar el disco entero.

- **[low]** `dashboard/views/heatmap_actividad.py:154, 221` — frp_timeline.json se lee y parsea dos veces por render (sin cache)
  - _load_frp_timeline() lee FRP_TIMELINE_PATH y hace json.loads sin cache. render() lo invoca dos veces por pasada: en _render_frp_timeline_section (l.154) y en render() para el roll-up diario (l.221). El archivo tiene 234 scans y crece; cada render parsea el JSON completo dos veces.
  - **Fix:** Decorar _load_frp_timeline con `@st.cache_data(ttl=600, show_spinner=False)` (el workflow lo regenera cada hora), o leer el JSON una sola vez en render() y pasar el dict a _render_frp_timeline_section.

- **[low]** `dashboard/views/volcat_viewer.py:733-734` — Rama not-fetched de _render_ssec_rgb_block llama get_latest_time sin cache en cada rerun
  - En _render_ssec_rgb_block la rama default (botón no pulsado) llama el get_latest_time('ash_rgb')/('so2_rgb') crudo y sin cache (l.733-734), mientras la rama fetched usa los wrappers cacheados _get_latest_time_cached. Como el expander es parte de la página, la versión sin cache dispara dos round-trips a RealEarth /latest en cada rerun solo para mostrar timestamps, anulando los wrappers de cache.
  - **Fix:** Usar `_get_latest_time_cached('ash_rgb')` y `_get_latest_time_cached('so2_rgb')` también en la rama not-fetched, para que ambas compartan el TTL_VOLCAT.

- **[low]** `src/fetch/timeseries.py:201-202` — Import muerto ts_to_parts dentro de _one, re-ejecutado por cada frame del ThreadPool (hasta 144×)
  - _one (clausura por cada ts en el ThreadPool) hace `from src.fetch.rammb_slider import ts_to_parts` y `from dashboard.utils import parse_rammb_ts` en su cuerpo. ts_to_parts NO se usa dentro de _one (import muerto); parse_rammb_ts sí. Para n_frames=144 son 144 ejecuciones del import (baratas tras la primera por sys.modules, pero desprolijo). El lazy import de parse_rammb_ts es intencional (CLAUDE.md), pero ts_to_parts sobra.
  - **Fix:** Eliminar el import muerto ts_to_parts. Mantener parse_rammb_ts lazy pero moverlo fuera de _one al cuerpo de fetch_volcano_timeseries (1 import en vez de 144), conservando el patrón lazy dentro de la función.

- **[low]** `src/fetch/goes_fdcf.py:194, 313` — S3FileSystem(anon=True) nuevo por llamada en las funciones FDCF en vez de reusar el singleton
  - fetch_latest_hotspots (l.194), fetch_hotspots_at_time (l.313) y frp_timeline.fetch_scan_sliced (l.188) instancian `s3fs.S3FileSystem(anon=True)` en cada invocación. goes_s3.py ya tiene un singleton _get_fs() con el mismo propósito. Crear uno nuevo reinicializa el cliente botocore/aiobotocore y descarta el cache de listados/conexiones; en un barrido de timeline (frp_timeline en loop) eso multiplica el setup. s3fs cachea por kwargs a nivel de clase, así que el costo no es enorme, pero el patrón consistente es reusar el singleton.
  - **Fix:** Exponer y reusar el singleton de S3FileSystem (mover _get_fs a un helper compartido o importarlo) en las tres funciones FDCF.

- **[low]** `dashboard/views/comparador.py:413-425` — Modo Baseline: _frame_robust prueba zoom=4 sobre ~25 timestamps históricos antes de caer a zoom=3
  - _build_target_ts_window genera ~25 timestamps y _frame_robust los pasa completos a fetch_frame_robust con zoom_preferred=4. fetch_frame_robust itera TODOS los ts en zoom 4 hasta que uno cargue, y solo si ninguno carga reintenta TODOS en zoom 3. Para una baseline histórica donde el tile zoom=4 suele no existir (RAMMB adelgaza historia), se disparan hasta 25 mosaicos zoom=4 que fallan y luego 25 en zoom=3 — el peor caso de latencia de la vista (~50 fetch-de-mosaico secuenciales).
  - **Fix:** Acotar a los ts más cercanos al target: pasar `baseline_window[:7]` (ya ordenados por proximidad en _build_target_ts_window) a _frame_robust.

- **[low]** `scripts/build_backfill.py:230-235` — _build_volcat sin paralelismo ni dedup de frame entre scopes — N descargas RealEarth seriales
  - _build_volcat itera `for ts: for sid in scopes:` con un fetch_image('ash_rgb', ...) síncrono por cada (ts, scope). Con 51 ts × 2 scopes = 102 descargas seriales. A diferencia de _build_hotspots (cachea por ts) y _build_volcat_height (dedup por frame_dt), aquí el mismo frame VOLCAT se baja una vez por scope. Es opt-in (--include-volcat) así que el impacto es acotado, pero suma minutos al build de 30 min de timeout.
  - **Fix:** Cachear la respuesta de fetch_image por (ts, sector) entre scopes si el bbox de zona contiene al de volcán, o paralelizar con ThreadPoolExecutor(max_workers=3-4) respetando el rate limit de RealEarth.


## 🔬 Ciencia (6)

- **[medium]** `src/volcanos.py:58-74` — Ranking SERNAGEOMIN duplicado (4 volcanes con rank 3) y rangos faltantes (23, 24, salta a 25)
  - El Ranking de Riesgo Específico SERNAGEOMIN 2019 es una ordenación estricta y única 1..N. En el catálogo: rank 3 está asignado a 4 volcanes (Puyehue-Cordón Caulle, Calbuco, Chaitén, Hudson); faltan los rangos 23 y 24; el máximo salta a 25 (Ollagüe). Indica errores de transcripción. Impacto operacional bajo hoy (el campo .ranking no se usa en ningún módulo), pero es dato científico incorrecto que induciría a error si se empieza a usar para priorización/ordenamiento.
  - **Fix:** Re-cotejar contra el listado oficial SERNAGEOMIN 2019 y asignar posiciones únicas. Verificar especialmente Calbuco, Chaitén, Hudson y Puyehue, que no pueden compartir posición. Completar 23/24.

- **[low]** `src/process/hires_pipeline.py:240-243` — Upscale de banda 13 (2km) a 1km con np.repeat introduce offset de ~0.5 px (1 km) vs grid de banda 1
  - bt13 = np.repeat(np.repeat(bt13_2km,2,axis=0),2,axis=1) replica cada píxel 2x2 y se recorta a lat.shape (grid 1km de banda 1). En el fixed-grid ABI el centro del píxel 2km no coincide con el centro del par 1km: la repetición desplaza la imagen ~0.5 px (1 km) hacia una esquina y no interpola. Combinado con lat/lon de banda 1, produce un sesgo sistemático de ~1 km en la georef del IR nocturno, relevante para localizar plumas/hot spots sobre cráteres de ~500 m.
  - **Fix:** Recortar la banda 13 a su propio bbox con su propio lat/lon (get_lat_lon del dataset de banda 13), o usar interpolación bilineal centrada (scipy.ndimage.zoom order=1). Como mínimo, documentar en el docstring que el IR nocturno tiene ~1 km de incertidumbre de registro.

- **[low]** `src/process/geocolor_lite.py:182-184` — solar_elevation omite la ecuación del tiempo; el error real cerca del terminador supera el ~0.5° declarado
  - El docstring promete ~0.5° pero el cálculo usa hora solar media sin la ecuación del tiempo (±16 min) ni refracción. ±16 min = hasta ~4° de error en el ángulo horario; cerca del amanecer/atardecer (sol ~0.25°/min en elevación) se traduce en varios grados de error justo en el rango crítico del switch día/noche (DAY_NIGHT_THRESHOLD_DEG=5°). Consecuencia: scopes pueden conmutarse a IR pseudo-color/visible hasta ~15 min antes/después de lo correcto. No catastrófico (solo decide qué render mostrar) pero el '~0.5°' es optimista.
  - **Fix:** Añadir la ecuación del tiempo (aprox. Spencer/NOAA, EoT como función del día del año), o documentar honestamente que la precisión real es ~±2-4° cerca del terminador y que por eso el umbral se fijó conservador en 5°.

- **[low]** `dashboard/views/modo_guardia_volcan.py:345-347` — Azimut de la punta de flecha de viento no corregido por el pre-stretch 1/cos(lat) — head y shaft no alinean
  - La LÍNEA de la flecha de viento divide la componente lon por cos(lat) (_wind_arrow_endpoints l.167) para compensar el scaleratio=1/cos(lat), así apunta en la dirección visualmente correcta. Pero el ángulo del marcador de PUNTA se computa del vector crudo: `angle=degrees(arctan2(w['u'], w['v']))` (l.345), sin el factor cos(lat). Como el eje lon está visualmente comprimido por cos(lat), el azimut de la punta queda rotado respecto a la línea que remata (error crece con |u|/|v| y latitud; Villarrica ~-39.4°, cos≈0.77). Puramente cosmético (la línea es correcta) pero head y shaft pueden discrepar visiblemente para vientos zonales fuertes.
  - **Fix:** Computar el ángulo de la punta con las mismas componentes estiradas de la línea: `angle = degrees(arctan2(u/cos_lat, v))` con `cos_lat = cos(radians(volcan_lat))`.

- **[low]** `src/process/ash_detection.py:122-128` — Umbral de baja confianza (-0.5K) hardcodeado fuera de config, rompiendo la single-source-of-truth de umbrales Prata
  - compute_ash_confidence hardcodea el umbral de baja confianza -0.5 K (línea 123), mientras BTD_ASH_THRESHOLD (-1.0) y BTD_TRI_THRESHOLD (0.0) vienen de config. Eso rompe la single-source-of-truth de umbrales y hace que un ajuste requiera editar código en vez de config. El docstring documenta -0.5 pero no hay constante.
  - **Fix:** Mover -0.5 a una constante en src/config.py (p.ej. BTD_ASH_LOW_CONF_THRESHOLD = -0.5) y referenciarla, junto a las otras, para que los tres niveles de confianza tengan sus umbrales centralizados y versionados con las refs (Prata 1989 / ATBD).

- **[low]** `src/fetch/frp_timeline.py:84-87` — Distancia volcán-hotspot usa cos(lat) del volcán (equirectangular) — válido solo para radius_km≥50
  - sum_frp_per_volcano aproxima la distancia con `dlat=(h.lat-v.lat)*111`, `dlon=(h.lon-v.lon)*111*cos(v.lat)`. Es la aproximación equirectangular estándar, razonable para radius_km=50 (error ~1-2 km vs haversine a lat -53, despreciable). NO es un bug operacional con radius=50. Nota científica de bajo impacto: si en el futuro se baja radius_km a <10 km (atribución fina), conviene migrar a haversine.
  - **Fix:** Dejar como está para radius_km≥50. Si se reduce el radio, migrar a haversine real o usar el promedio cos((v.lat+h.lat)/2). Documentar el límite de validez en el docstring.


## 🛡 Robustez (27)

- **[high]** `dashboard/views/comparador.py:294-299` — Modo '2 Volcanes': cambiar Volcán 1 al valor de Volcán 2 deja un valor stale en session_state → StreamlitAPIException
  - En _mode_dos_volcanes, v2_options se construye excluyendo v1 (línea 295), pero el selectbox de Volcán 2 usa key fija `comp_2v_v2`. Streamlit persiste el valor por key entre reruns y, cuando ese valor ya no está en options, lanza StreamlitAPIException ('The default value is not part of the options'). Reproducible: v1=Villarrica, v2=Lascar; el usuario cambia v1 a Lascar → v2_options excluye Lascar pero comp_2v_v2 sigue valiendo 'Lascar' → excepción no capturada que rompe la pestaña. Patrón clásico de selectboxes acoplados con opciones dinámicas.
  - **Fix:** Tras calcular v2_options, si `st.session_state.get('comp_2v_v2') not in v2_options: st.session_state.pop('comp_2v_v2', None)` antes de instanciar el selectbox.

- **[high]** `scripts/deploy_hf.sh:19, 63-69` — set -e + sin trap deja al usuario en el orphan branch si falla el push
  - El script corre con set -e (línea 19). Crea un orphan branch temporal, hace git push --force a HF (líneas 63-65) y SÓLO DESPUÉS vuelve a main y borra el branch temporal (67-69). Si el push falla (token expirado, red, rechazo por tamaño, rate limit), set -e aborta ANTES de la limpieza: el usuario queda atascado en el orphan branch hf-deploy-<ts> con el working tree en estado de snapshot. Re-correr acumula branches huérfanos. Para un usuario que no es git-power-user es un pie de tabla confuso.
  - **Fix:** Agregar trap de limpieza al inicio: `cleanup() { git checkout main -f 2>/dev/null || true; git branch -D "$TEMP_BRANCH" 2>/dev/null || true; }` y `trap cleanup EXIT`. Quitar entonces las líneas 67-69 redundantes. Al inicio, podar branches viejos `hf-deploy-*` de corridas previas abortadas.

- **[medium]** `src/process/geocolor_lite.py:42-44, 83, 123` — rad_to_reflectance preserva NaN y rompe los stretches por percentil (panel TrueColor negro cerca del terminador)
  - ABI L1b enmascara fuera-de-disco/saturados a NaN al abrir. rad_to_reflectance hace `np.clip(rad.values * kappa0, 0, 1)` y np.clip preserva NaN. Ese array llega a contrast_stretch (np.percentile, línea 83) y band2_monochrome_05km (línea 123): np.percentile (no nanpercentile) devuelve NaN con un solo NaN → `(ch-lo)/(hi-lo)` se vuelve NaN para todo el canal → al castear a uint8 da basura/negro. En crops diurnos en tierra rara vez aparece NaN, pero en bordes de scope cerca del terminador o regiones que tocan el limbo, el panel TrueColor sale negro sin error visible. rad_to_bt sí lo maneja (rad.where(rad>0)); la rama de reflectancia no. unsharp_mask/gaussian_filter también propagan NaN.
  - **Fix:** Usar np.nanpercentile en contrast_stretch y band2_monochrome_05km; en rad_to_reflectance sustituir NaN tras el clip (`np.nan_to_num(np.clip(rad.values*kappa0,0,1), nan=0.0)`) o propagar máscara explícita como rad_to_bt. Limpiar NaN antes de unsharp_mask.

- **[medium]** `src/fetch/wind_data.py:65, 100` — wind_data usa requests.get pelado en vez de la session compartida — sin retry ni keep-alive bajo 12 threads
  - fetch_wind_point (l.65) y fetch_wind_diagnostic (l.100) llaman `requests.get(...)` directo, NO get_session() de _http_session. fetch_wind_grid los dispara con ThreadPoolExecutor(max_workers=12) sobre ~50 puntos. Consecuencias: (1) sin reintentos con backoff ante 429/500/502/503/504, y Open-Meteo aplica rate-limiting agresivo (429) cuando se le pegan 50 requests casi simultáneos → un 429 transitorio descarta ese punto y deja huecos en el campo de viento; (2) sin connection pooling: abre/cierra TLS 50 veces por refresco. _http_session existe justamente para unificar esto; wind_data quedó fuera de la migración.
  - **Fix:** `from src.fetch._http_session import get_session` y reemplazar `requests.get(...)` por `get_session().get(...)` en ambas funciones. Hereda retry+backoff (mitiga 429) y keep-alive para la grilla paralela.

- **[medium]** `src/fetch/volcat_api.py:249` — volcat_at_time lanza TypeError si target_dt es naive (resta aware - naive), descartando VOLCAT histórico en silencio
  - _parse_volcat_frame_dt siempre devuelve datetime aware (utc). En `gap = abs((fdt - target_dt).total_seconds())` (l.249), si target_dt llega naive Python lanza `TypeError: can't subtract offset-naive and offset-aware`. El único caller actual (build_backfill.py:307) pasa aware y su try/except traga la excepción, pero el efecto es que se descartan SILENCIOSAMENTE todos los frames de ese ts sin log claro. Trampa latente: cualquier caller futuro con dt naive (común en el dashboard) verá VOLCAT histórico vacío sin causa aparente.
  - **Fix:** Normalizar al inicio de volcat_at_time, como ya hacen fetch_scan_sliced/fetch_hotspots_at_time: `if target_dt.tzinfo is None: target_dt = target_dt.replace(tzinfo=timezone.utc)`.

- **[medium]** `dashboard/style.py:429-441` — El watchdog escanea TODOS los [role=dialog] del documento padre — riesgo de reload espurio
  - hasConnError() hace querySelectorAll('[role="dialog"], [data-testid="stConnectionStatus"]') sobre el documento padre y concatena innerText de todos los diálogos, matcheando regex de error de websocket. Cualquier modal del usuario (fullscreen de un plotly, settings, st.dialog) que contenga 'Connection error' y 'Websocket' dispararía el reload; o el propio diálogo de error de Streamlit ofreciendo 'Rerun'/'Connecting' mientras reconecta solo. El backoff de 60s limita pero no elimina el reload espurio. Además el setInterval corre indefinidamente en cada component iframe; como components.html re-monta el iframe en cada rerun, pueden acumularse intervalos vivos.
  - **Fix:** Estrechar el selector a contenedores de error reales de Streamlit ([data-testid="stException"], [data-testid="stConnectionStatus"]) y exigir el patrón completo y específico ('Cached ForwardMsg'/'Failed to process a Websocket message') en vez del match laxo. Guardar el id del interval previo y limpiarlo antes de crear uno nuevo.

- **[medium]** `src/fetch/historic_rammb.py:179-185` — fetch_historic_stitched asume tiles homogéneos: un tile de tamaño distinto rompe el stitch con broadcast error
  - fetch_historic_stitched detecta tile_sz del primer tile y coloca cada tile con slicing fijo `canvas[i*tile_sz:(i+1)*tile_sz, ...] = tiles[(r,c)]`. Si RAMMB devuelve un tile de borde recortado, o un tile 678 mezclado con 512 según producto/zoom (caso que el código NRT advierte y maneja), esa asignación lanza ValueError por broadcast incompatible y tira toda la imagen histórica. El gemelo NRT fetch_stitched_frame (rammb_slider.py:398-408) SÍ tiene el safety (th,tw = arr.shape; ch=min(th,tile_sz); cw=min(tw,tile_sz)); esta versión histórica no lo copió.
  - **Fix:** Replicar el clamp de fetch_stitched_frame: `th,tw = arr.shape[:2]; ch=min(th,tile_sz); cw=min(tw,tile_sz); canvas[y0:y0+ch, x0:x0+cw] = arr[:ch,:cw]`. Un tile anómalo degrada en vez de abortar.

- **[medium]** `dashboard/views/heatmap_actividad.py:98-99` — _build_frp_timeline_fig asume orden cronológico y formato exacto de timestamps sin validar
  - La función parsea `times` con `datetime.strptime(s['t'], '%Y-%m-%dT%H:%M:%SZ')` para cada scan sin try/except. Si un solo registro de frp_timeline.json tiene un 't' con formato distinto o ausente (corrupción parcial, cambio futuro del workflow), strptime lanza ValueError/KeyError y el panel primario (el más importante) cae con excepción no atrapada, rompiendo la página. Además span_h y el eje X asumen `scans` cronológico (times[-1]-times[0]); scans desordenados darían span_h negativo. _load_frp_timeline atrapa el JSON corrupto a nivel archivo pero no valida la estructura interna de cada scan.
  - **Fix:** Envolver el parseo de cada 't' en try/except (descartar scans inválidos y loguear) y ordenar `scans = sorted(scans, key=lambda s: s.get('t',''))` antes de graficar.

- **[medium]** `scripts/build_frp_timeline.py:71-74, 59-68` — save() no es atómico — un corte durante write_text corrompe frp_timeline.json y borra todo el histórico
  - save() hace `OUTPUT_PATH.write_text(json.dumps(...))` directo. Si el proceso se mata a mitad de escritura (runner cancelado, OOM, timeout justo en el write), el archivo queda truncado. La próxima corrida lo detecta en load_existing, que ante json.loads fallido loguea warning y devuelve `{'scans': []}` — descartando SILENCIOSAMENTE la ventana de 48h Y los 30 días de roll-up diario, que solo se reconstruyen con un bootstrap manual --rollup-days. Como el workflow corre cada 15 min y commitea, una corrupción se 'auto-sana' perdiendo todo el histórico del heatmap. Este archivo es la FUENTE ÚNICA del heatmap intradía y semanal (CLAUDE.md).
  - **Fix:** Escritura atómica: `tmp = OUTPUT_PATH.with_suffix('.json.tmp'); tmp.write_text(...); os.replace(tmp, OUTPUT_PATH)`. Y antes de regenerar desde cero en load_existing, renombrar el corrupto a `.bak` para no perder data recuperable manualmente.

- **[medium]** `scripts/build_frp_timeline.py:160-187` — Backfill histórico del roll-up es secuencial sin paralelismo — puede colgar el job de 20 min y dejar datos parciales
  - El bloque --rollup-days barre [now-rollup_days, now-2días] a rollup_step_min (default 20 min) SECUENCIAL y SÍNCRONO (while loop), un fetch_scan_sliced (~1-2s S3 c/u) por iteración, sin ThreadPoolExecutor. Para 30 días a 20 min = 2160 scans × ~2s ≈ 72 min. El workflow tiene timeout-minutes: 20 (frp_timeline.yml:27): el bootstrap se mata a mitad, dejando el roll-up parcial y commiteado (días a medio rellenar), sin checkpoint ni reanudación. Riesgo: que se corra en el workflow programado y se trunque en silencio.
  - **Fix:** Paralelizar con ThreadPoolExecutor (3-4 workers contra S3, como _build_rammb) y/o documentar en el help de --rollup-days que debe correrse LOCAL (no en el runner de 20 min) para datasets grandes. Considerar un guard que aborte si la estimación supera el timeout.

- **[medium]** `scripts/build_backfill.py:385-386` — Limpieza de OUT_DIR con p.unlink() falla si hay subdirectorios
  - La limpieza inicial hace `for p in OUT_DIR.glob('*'): p.unlink()`. Path.unlink() lanza IsADirectoryError (o PermissionError en Windows) si p es un directorio. Hoy solo se escriben archivos planos, pero es frágil: si queda un subdir (__pycache__, .ipynb_checkpoints, o restos de una versión futura), la limpieza explota antes de empezar y aborta todo el build. El mismo patrón aparece en build_hires_cache.py:65 y build_animation_cache.py:141-142.
  - **Fix:** `import shutil; for p in OUT_DIR.glob('*'): (shutil.rmtree(p) if p.is_dir() else p.unlink())`, o `shutil.rmtree(OUT_DIR, ignore_errors=True); OUT_DIR.mkdir(parents=True, exist_ok=True)`.

- **[medium]** `scripts/build_animation_cache.py:118-135` — _build_bounds escribe un ZIP vacío cuando RAMMB no devuelve ningún frame, borrando el cache bueno previo
  - En _build_bounds, si get_latest_timestamps devuelve timestamps pero todos los fetch_frame_for_bounds devuelven None (RAMMB caído intermitente), `collected` queda vacío y aun así se llama _save_frames_zip(scope_id, product, {}), creando un ZIP de 0 frames. Como la action borra todos los assets viejos antes de subir, ese ZIP vacío reemplaza a uno previo que SÍ tenía frames → una caída transitoria de RAMMB borra el cache de ese scope/producto hasta la próxima corrida exitosa. _build_nacional sí evita esto (early-return si `not frames`); _build_bounds no chequea collected vacío.
  - **Fix:** Antes de _save_frames_zip en _build_bounds, early-return si `not collected`: `if not collected: log.warning(...); return []`. Igual que _build_nacional.

- **[medium]** `scripts/deploy_hf.sh:41-58` — git add con `2>&1 | tail -3` enmascara fallos de archivos faltantes bajo set -e
  - El git add de archivos esenciales pipea `2>&1 | tail -3`. Esto rompe set -e de dos formas: (1) el exit status del pipe es el de tail (siempre 0), no el de git add; (2) si un archivo esencial no existe (data/frp_timeline.json aún no generado en un clon fresco, requirements_actions.txt renombrado), git add emite warning pero el deploy sigue y sube un Space INCOMPLETO que no arranca, con el error oculto en las últimas 3 líneas del pipe. Deploy silenciosamente roto.
  - **Fix:** Quitar el `2>&1 | tail -3` para que git add falle ruidosamente bajo set -e, o validar la existencia de cada path crítico antes del add (`for f in ...; do [ -e "$f" ] || { echo "FALTA $f"; exit 1; }; done`).

- **[medium]** `dashboard/views/zonas_fullscreen.py:703-708, 1323-1328` — Slot TV se queda en placeholder permanente si el productor nunca produce ese slot (cache frío + restart)
  - _render_volcan_zoom_tv y _render_4_zonas_image_tv solo leen de _TV_PRODUCED y muestran placeholder si la clave falta. El productor compone eumetsat_ash al FINAL de _produce_once (que serializa los otros productos, VOLCAT y zooms). Tras un restart con cache frío, eumetsat_ash tarda ~54s en componerse; si _produce_once total excede los ~20s del período, el slot recién aparece tras 1-2 ciclos. Mientras, como idx es por reloj, al slot de Ash igual le toca su turno y muestra placeholder. El peor caso post-restart muestra 'Preparando…' por más de un ciclo de rotación completo en una sala 24/7.
  - **Fix:** Mantener un cache de 'último PNG bueno' por slot y mostrarlo en el placeholder en vez de texto, para evitar la pantalla 'Preparando' visible. Alternativamente, componer eumetsat_ash en su propio sub-hilo o reducir TV_PRODUCER_PERIOD_S durante el arranque frío.

- **[low]** `dashboard/views/timeseries_viewer.py:326` — Cambiar producto/ventana sin pulsar 'Calcular' deja KPIs y serie con producto previo (estado UX desincronizado)
  - El fetch está gated por botón (`if fetch or 'ts_data' not in st.session_state`). Tras el primer cálculo, si el usuario cambia el selectbox de Producto/Volcán/Ventana sin pulsar Calcular, la página sigue mostrando cur del cómputo anterior. Las etiquetas usan cur almacenado, así que NO hay mislabeling, pero los selectores muestran un estado y el gráfico otro sin pista visual de que falta re-calcular. En crisis puede llevar a leer la serie del volcán equivocado creyendo que es el seleccionado.
  - **Fix:** Mostrar un aviso cuando los selectores difieren de cur: `if (volc_name, product, n_frames, radius) != (cur[...]): st.warning('Cambiaste los parámetros — pulsá Calcular')`, o atenuar el plot hasta recalcular.

- **[low]** `dashboard/views/volcat_viewer.py:582` — KPI 'Hora del scan' crashea si meta['datetime'] es None (key presente, valor None)
  - `short_ts = meta.get('datetime', '—').split('_')[-1]...` (l.582). volcat_latest setea `'datetime': last.get('datetime')`, que puede ser None cuando el frame SSEC carece del campo. Como la key EXISTE con valor None, `meta.get('datetime', '—')` devuelve None (no el default), y `None.split('_')` lanza AttributeError, crasheando la página. La línea 575 (_parse_volcat_dt(meta.get('datetime'))) ya maneja None; solo esta ruta .split está desprotegida.
  - **Fix:** `dts = meta.get('datetime') or ''` y `short_ts = dts.split('_')[-1].replace('-', ':') if dts else ''`, conservando el fallback a '—'.

- **[low]** `dashboard/views/volcat_viewer.py:278-279` — _volcat_colorbar_strip: fracciones de recorte hardcodeadas a un único layout SSEC (1000x821), sin validación
  - El colorbar se recorta con fracciones fijas `crop = im.crop((int(0.16*w), int(0.915*h), int(0.875*w), int(0.997*h)))`, medidas sobre el layout SSEC 1000x821. Asumen aspect/layout fijo. Si SSEC sirve un sector con aspecto distinto (las 4 zonas ya usan Chile_North/Central/South con extensión N-S diferente) o revisa el footer, esto recorta la región equivocada y muestra un 'colorbar' basura. No hay validación de que la banda recortada contenga la escala arcoíris; la función traga la excepción (return b'') en silencio. Feature reciente de alto riesgo (commit a46b953).
  - **Fix:** Keyear las fracciones al tamaño real con tolerancia, o detectar la banda del colorbar escaneando el gradiente arcoíris; al menos assert/log cuando el aspect w/h se desvía del ~1000x821 calibrado, para que el fallo sea visible.

- **[low]** `src/fetch/realearth_api.py:115` — fetch_image no valida que la respuesta sea imagen: error HTML/JSON con status 200 cae a except sin diagnóstico
  - fetch_image hace `Image.open(io.BytesIO(resp.content))` directo tras raise_for_status. Si RealEarth devuelve 200 con un cuerpo de error (HTML de mantenimiento o JSON {'error':...} con status 200, ha pasado), PIL lanza UnidentifiedImageError, que cae en el except genérico y loguea solo 'Error descargando imagen' sin pista de que el cuerpo no era imagen. Degrada a None correctamente pero el log es poco accionable.
  - **Fix:** Antes de abrir, chequear `resp.headers.get('content-type','').startswith('image')`; si no, loguear `resp.text[:200]` y devolver None.

- **[low]** `src/export/geotiff.py:31-32, 83, 112` — nodata=0 por defecto marca como transparente cualquier píxel RGB legítimamente negro
  - build_geotiff_bytes usa nodata=0 por defecto aplicado a las 3 bandas. En un GeoTIFF RGB, rasterio interpreta nodata por banda: cualquier píxel con 0 en una banda (sombras profundas, terreno volcánico oscuro, negro saturado) se vuelve transparente en QGIS, no solo el padding de tile. El docstring documenta la intención, pero para Ash RGB/visible con negros legítimos puede ocultar datos reales. build_geotiff_from_rgb (l.112) además no expone el parámetro nodata.
  - **Fix:** Considerar nodata=None por defecto y dejar que el caller lo active, o usar una banda alpha de máscara en vez de nodata=0 sobre las 3 bandas. Como mínimo, exponer nodata en build_geotiff_from_rgb para paridad.

- **[low]** `dashboard/views/live_viewer.py:1112` — Fallback de bounds usa CHILE_TILE_BOUNDS pero el frame siempre trae CHILE_REPROJECTED_BOUNDS
  - `bounds = frame.get('bounds', CHILE_TILE_BOUNDS)`. _fetch_frame_for_ts siempre setea frame['bounds']=CHILE_REPROJECTED_BOUNDS, así que el .get jamás cae al default. Pero si por refactor futuro el frame llegara sin 'bounds', el fallback usaría CHILE_TILE_BOUNDS (los bounds de los tiles geoestacionarios, distintos de los reprojectados), produciendo georef incorrecta del overlay y volcanes desplazados. Es un fallback que apunta al dato equivocado.
  - **Fix:** Cambiar el default a CHILE_REPROJECTED_BOUNDS para que coincida con lo que produce _fetch_frame_for_ts, o acceder directo a frame['bounds'] (ya garantizado).

- **[low]** `dashboard/views/rammb_viewer.py:945-1000` — Botones de descarga MP4/GIF/ZIP leen session_state global no ligado al scope/producto actual (bytes stale)
  - Los binarios se guardan en claves globales fijas '_mp4_bytes'/'_gif_bytes'/'_zip_bytes'. Si el usuario genera un MP4 para un scope/producto, cambia de animación y vuelve al expander, el download_button sigue ofreciendo los bytes viejos hasta regenerar. El nombre de archivo (base_name) sí refleja el nuevo scope, así que el usuario puede bajar un archivo cuyo contenido no corresponde al nombre mostrado — trampa de integridad de datos exportados.
  - **Fix:** Incluir un hash de (product, scope_label, ts_first, ts_last) en la key de session_state (`f'_mp4_{base_name}'`) o limpiar las keys de bytes cuando 'anim_sel' cambia, para que el botón solo aparezca para la animación vigente.

- **[low]** `scripts/generate_lascar_report.py:250-263` — --date sin validación amigable y --out-dir no se refleja en el commit del workflow
  - main() construye out_path desde args.date pero si se pasa --date con formato inválido o fecha futura, datetime.strptime lanza ValueError sin mensaje amigable. Además el workflow lascar_pdf.yml commitea reports/ fijo; con --out-dir distinto el PDF no se commitea. El script no valida la fecha ni avisa.
  - **Fix:** Envolver strptime en try/except con mensaje claro ('--date debe ser YYYY-MM-DD'). Documentar en el help de --out-dir que el workflow solo commitea reports/. Opcionalmente rechazar fechas futuras.

- **[low]** `dashboard/utils.py:33-37` — fmt_chile usa %Z que puede devolver string vacío en contenedores sin tzdata
  - fmt_chile hace `dt_ch.strftime('%Z')` para 'CLT'/'CLST'. En contenedores Docker minimalistas (HF Spaces cpu-basic) sin paquete tzdata o con locale C, %Z puede devolver cadena vacía, produciendo 'HH:MM ' (espacio colgante) en fmt_chile y fmt_both. No rompe pero ensucia los timestamps. map_helpers.py:294 usa el mismo patrón con try/except defensivo; utils.py no.
  - **Fix:** Derivar la etiqueta del offset UTC de forma determinista (UTC-3→CLST, UTC-4→CLT) o caer a una etiqueta calculada si %Z viene vacío. Asegurar tzdata en requirements.txt para el deploy Docker.

- **[low]** `dashboard/utils.py:54-60` — parse_rammb_ts no valida longitud/contenido — crash poco informativo ante input malformado
  - parse_rammb_ts asume exactamente 14 dígitos y hace slicing posicional + int(). Ante un string más corto o un valor inesperado de la API, `int(ts[10:12])` puede dar ValueError opaco. Se llama en ~15 sitios sobre datos externos de RAMMB/FDCF; un timestamp malformado tumba la vista entera (la mayoría de callers no lo envuelven en try/except).
  - **Fix:** Guardia al inicio: `if not (isinstance(ts, str) and len(ts) == 14 and ts.isdigit()): raise ValueError(f'timestamp RAMMB inválido: {ts!r}')`, dando un mensaje claro. Alternativamente devolver None y manejarlo en los sitios de uso.

- **[low]** `src/fetch/rammb_slider.py:313-319` — reproject_to_latlon valida solo |x_m|<1e10 (no y_m) para detectar puntos fuera del disco
  - El mask `valid` chequea isfinite(x_m) & isfinite(y_m) & (abs(x_m) < 1e10) pero NO acota |y_m|. Para puntos fuera del disco pyproj GEOS devuelve inf en ambas coords, así que isfinite(y_m) lo cubre en la práctica; pero el chequeo es asimétrico y fragiliza el filtro de borde si pyproj cambia de comportamiento (errcheck, versión).
  - **Fix:** Agregar la cota simétrica en y: `& (np.abs(y_m) < 1e10)`, para que ambos ejes se traten igual.

- **[low]** `dashboard/views/zonas_fullscreen.py:487-488` — _render_volcat_zonas_tv crea st.columns(len(specs)) sin proteger contra specs vacío
  - `cols = st.columns(len(specs))` con specs = _volcat_zone_specs(). Si ZONE_TO_SECTOR_4/ZONE_TO_SECTOR estuvieran vacíos (config rota, import parcial), st.columns(0) lanza StreamlitAPIException ('Columns must be greater than 0'). _volcat_zone_specs importa ZONE_TO_SECTOR_4 desde volcat_api dentro de la función sin guard. No probable en operación normal, pero un cambio futuro rompe el grid con traceback en vez de mensaje prolijo.
  - **Fix:** Guard temprano: `if not specs: st.info('VOLCAT sin zonas configuradas'); return` antes de st.columns.

- **[low]** `dashboard/views/zonas_fullscreen.py:528-534` — Fallback de view_bounds clásico no valida que el recorte produzca un rango lon positivo
  - En _render_volcat_one_zona_tv, con view_bounds None (modo 3 zonas/ssec clásico), se construye vb recortando lon a max(sb['lon_min'],-76.0)..min(sb['lon_max'],-66.0). Si el sector SSEC cayera fuera de ese rango, vb queda con lon_min >= lon_max → eje invertido/degenerado en plotly → figura vacía/rota sin mensaje. Hoy los sectores chilenos caen dentro, pero no hay guard.
  - **Fix:** Validar `if vb['lon_max'] <= vb['lon_min'] or vb['lat_max'] <= vb['lat_min']: vb = sb` (usar el sector completo como fallback) antes de pasar a _volcat_zone_fig.


## ♻ Duplicación (8)

- **[high]** `dashboard/views/volcat_viewer.py:249-270, 310-324, 360-370` — El mismo PNG VOLCAT (~730 KB) se descarga 2-3 veces por zona y por ciclo, con requests.get pelado
  - Para cada zona VOLCAT, en un mismo ciclo se baja el mismo image_url de SSEC por hasta tres caminos cacheados que NO comparten el download: (1) _volcat_image_bytes (usado por _volcat_colorbar_strip), (2) _volcat_map_only con su propio requests.get interno (l.360-370), (3) en ssec_overlay _volcat_image_with_overlays con OTRO requests.get (l.310-324). El PNG SSEC pesa ~730 KB. El foreground del flujo de 4 zonas llama _volcat_map_only + _volcat_colorbar_strip, y el productor los pre-calienta también: como _volcat_map_only no reusa _volcat_image_bytes, cada uno baja su copia → ~2x ~730 KB por zona × 4 = ~5.8 MB redundantes por refresco del productor (cada 20s) más el foreground. Además estos requests.get directos NO usan la session compartida con Retry/keep-alive de _http_session (cuyo docstring cita a SSEC por nombre): un único 503 transitorio devuelve b'' y el slot VOLCAT muestra 'sin frame'.
  - **Fix:** Hacer que _volcat_map_only y _volcat_image_with_overlays obtengan el PNG base vía el wrapper cacheado _volcat_image_bytes(image_url), de modo que el byte-payload grande se baje UNA vez por (image_url, TTL) y los tres consumidores lo reutilicen. Y reemplazar requests.get(...) por get_session().get(...) de _http_session en las tres funciones para heredar Retry + pooling.

- **[low]** `dashboard/views/zonas_fullscreen.py:347-369, 528-542` — Dos estrategias de colorbar VOLCAT divergentes entre grid de 4 zonas y rotación de 1 zona
  - El colorbar VOLCAT se renderiza de dos formas según el path: en _render_volcat_zone_cell (grid 4 col) se llama _volcat_zone_fig SIN legend_bytes y el colorbar va aparte con st.image(leg) debajo; en _render_volcat_one_zona_tv (rotación 1 zona) se pasa legend_bytes=leg y el colorbar se incrusta dentro de la figura. Resultado: el mismo producto se ve con el colorbar en posiciones/tamaños distintos según el modo, con dos rutas de código para lo mismo (mantenimiento doble).
  - **Fix:** Unificar: pasar legend_bytes=leg también en _render_volcat_zone_cell para que ambos paths incrusten el colorbar en la figura, y eliminar el st.image(leg) separado. O extraer un helper único que decida la colocación.

- **[low]** `src/cache_ttl.py:34` — TTL_WIND_GFS definido pero no importado: las vistas hardcodean ttl=3600
  - cache_ttl.py centraliza TTL_WIND_GFS=3600 con el objetivo de ajustar todas las llamadas en un solo lugar. Pero los caches de viento (live_viewer.py:414, modo_guardia_volcan.py:128, rammb_viewer.py:288, modo_guardia.py:574) hardcodean ttl=3600 en vez de importar la constante, contradiciendo la filosofía del módulo. TTL_TIMESTAMPS_LIST sí se importa en comparador.py.
  - **Fix:** Reemplazar los ttl=3600 hardcodeados por import de TTL_WIND_GFS desde src.cache_ttl (con el patrón lazy import + fallback de comparador.py:50-52).

- **[low]** `dashboard/views/zonas_fullscreen.py:69-71, 933` — Múltiples wrappers @st.cache_data de get_latest_timestamps con TTLs divergentes (30s vs 15s)
  - Conviven al menos tres caches del mismo get_latest_timestamps: _recent_ts (ttl 30, n=3) en zonas_fullscreen.py:69, _recent_timestamps (ttl 30, n=3) en modo_guardia_volcan.py:77, y _get_latest_ts/_fetch_latest_ts_all (ttl 15, n=1) en live_viewer.py. Cada uno es una entrada de cache separada que dispara su propio request a latest_times.json (con cache-buster → siempre al origen). Con Modo Sala + un panel abierto, se consultan los timestamps de los mismos productos por 2-3 caminos con ventanas distintas, multiplicando round-trips a RAMMB y arriesgando que distintas vistas vean ts levemente distintos.
  - **Fix:** Unificar en un único helper cacheado `latest_timestamps(product, n)` (en src/fetch o dashboard/utils) con un solo TTL acordado, importado por todas las vistas.

- **[low]** `dashboard/views/live_viewer.py:1289-1290` — Filtro de prioritarios con list-comprehension redundante en vez de get_priority()
  - `priority_names = [v.name for v in CATALOG if v.name in [p for p in PRIORITY_VOLCANOES]]`. El `[p for p in PRIORITY_VOLCANOES]` es una copia inútil (equivale a `if v.name in PRIORITY_VOLCANOES`). Además reimplementa lo que ya hace get_priority() (ya importado), aunque ordenado según CATALOG. Ruido que invita a divergencia si cambia la definición de prioritario.
  - **Fix:** Simplificar a `[v.name for v in get_priority()]` (fuente canónica) o como mínimo quitar la comprehension redundante: `if v.name in PRIORITY_VOLCANOES`.

- **[low]** `src/fetch/frp_timeline.py:210, 239` — box = bounds or CHILE_BBOX calculado dos veces en fetch_scan_sliced
  - En fetch_scan_sliced se evalúa `box = bounds or CHILE_BBOX` en l.210 (recorte de índices) y otra vez idéntico en l.239 (filtro final lat/lon). Duplicación inocua (mismo valor). El segundo filtro lat/lon es correcto y deseable como filtro fino; solo la doble evaluación de box es limpieza menor.
  - **Fix:** Calcular `box = bounds or CHILE_BBOX` una sola vez al inicio y reusar la variable.

- **[low]** `dashboard/views/mosaico_chile.py:394-411` — _grid_fragment_tv duplica el loop de grid 2x4 de _grid_fragment sin compartir helper
  - _grid_fragment y _grid_fragment_tv construyen ambos la grilla 2x4 de PRIORITY_VOLCANOES con el mismo patrón (rows split, loop, st.columns(4), get_volcano, _volcano_frame_with_fallback, _render_mini). Las diferencias son menores (target_width_px, badges hi-res, contadores). La lógica de fetch+render por celda está copiada casi literal, duplicando el riesgo de drift.
  - **Fix:** Extraer un helper `_render_priority_grid(product, timestamps, target_width_px, ...)` que encapsule el doble for y la llamada a _render_mini, parametrizando width y flags hi-res.

- **[low]** `src/process/pipeline.py:28` — Doble clip+escala de Ash RGB y contrato de salida no documentado
  - generate_ash_rgb/generate_ash_so2_rgb ya devuelven floats en [0,1] (normalize hace np.clip), pero _save_image repite `np.clip(rgb,0,1)*255`. Idempotente, pero oculta una inconsistencia de contrato: hires_pipeline/historic devuelven uint8 (0-255) mientras pipeline.process_ash_rgb maneja floats [0,1]. Si alguien pasara por error un array uint8 a _save_image, el clip a [0,1] lo aplastaría a 0/1 y *255 daría imagen casi negra. El contrato de _save_image no está documentado.
  - **Fix:** Documentar en _save_image que espera float [0,1], o detectar dtype (si uint8, no reescalar). Unificar el contrato de salida de los generadores RGB.


## 🧹 Dead code (13)

- **[low]** `dashboard/views/live_viewer.py:100-105, 543-561` — _fetch_latest_frame y _reloj_chile muertas + imports sin uso tras la consolidación del commit 9b9cebc
  - _fetch_latest_frame no se llama (se usa _get_latest_ts + _fetch_frame_for_ts directo). _reloj_chile tampoco (la consolidación movió el reloj a _health_banner pero dejó la función vieja); como es el único consumidor de now_utc, ese import queda muerto. Imports nunca usados: info_panel, kpi_card, refresh_info_badge, fmt_both_long, utc_to_chile, HotSpot. También los docstrings citan TTLs inexistentes (30s/90s) cuando el cache real de _get_latest_ts es 15s.
  - **Fix:** Borrar _fetch_latest_frame y _reloj_chile; limpiar imports (now_utc, fmt_both_long, utc_to_chile, info_panel, kpi_card, refresh_info_badge, HotSpot). Unificar los docstrings a 15s o eliminarlos al borrar la función muerta. Mantener fmt_chile, header.

- **[low]** `dashboard/views/rammb_viewer.py:22-37` — Imports sin uso en rammb_viewer.py (C_ASH, C_SO2, fmt_both, CHILE_BOUNDS, CHILE_TILE_BOUNDS, PRODUCTS, ts_to_parts)
  - Verificado por grep: no se usan C_ASH/C_SO2 (l.22, solo C_ACCENT), fmt_both (l.25), CHILE_BOUNDS (l.26), CHILE_TILE_BOUNDS y PRODUCTS (l.28), ts_to_parts (l.31). Imports muertos que cargan superficie cross-package innecesaria justo en el bloque try/except frágil de Streamlit Cloud (más símbolos = mayor probabilidad de que un hot-reload race rompa el import top-level).
  - **Fix:** Eliminar C_ASH, C_SO2, fmt_both, CHILE_BOUNDS, CHILE_TILE_BOUNDS, PRODUCTS, ts_to_parts. Mantener los símbolos efectivamente usados.

- **[low]** `dashboard/views/ash_viewer.py:13` — Imports sin uso en ash_viewer.py (C_ASH, C_SO2)
  - C_ASH y C_SO2 se importan pero no se usan: los markers usan C_ACCENT y las colorscales vienen de BTD_COLORSCALE/CONF_COLORSCALE/SO2_COLORSCALE. Símbolos muertos en el bloque try/except cross-package frágil.
  - **Fix:** Quitar C_ASH y C_SO2 de la línea 13. Conservar C_ACCENT.

- **[low]** `dashboard/views/replay_reciente.py:27, 35, 39` — Imports muertos en replay_reciente.py (datetime/timedelta/timezone, fmt_chile, CATALOG)
  - pyflakes confirma: `from datetime import datetime, timedelta, timezone` (l.27) sin uso; fmt_chile (l.35) sin uso; CATALOG (l.39) sin uso (solo get_volcano). Símbolos importados nunca referenciados.
  - **Fix:** Eliminar el import de datetime/timedelta/timezone, quitar fmt_chile del import de dashboard.utils (dejar parse_rammb_ts), y quitar CATALOG de src.volcanos (dejar get_volcano).

- **[low]** `dashboard/views/mosaico_chile.py:18, 367-368, 384` — Imports locales muertos (io, base64) y f-string sin placeholders en _grid_fragment_tv
  - En _grid_fragment_tv, las líneas 367-368 importan io y base64 localmente sin uso (la conversión a data URL la hace _array_to_data_url). La línea 384 tiene un f-string sin placeholders {}, así que el prefijo f es inútil. En el import top-level (l.18), fmt_chile se importa sin uso.
  - **Fix:** Borrar `import io` e `import base64`, quitar el prefijo f de la string de la l.384, y quitar fmt_chile del import de dashboard.utils.

- **[low]** `dashboard/views/timeseries_viewer.py:13, 21, 29` — Imports muertos en timeseries_viewer.py (timedelta, C_ASH, C_SO2, ZOOM_VOLCAN)
  - pyflakes confirma: `from datetime import timedelta` sin uso; C_ASH y C_SO2 (l.21) nunca referenciados (solo C_ACCENT); ZOOM_VOLCAN (l.29) importado pero las series/thumbnails usan ZOOM_ZONE.
  - **Fix:** Eliminar timedelta, C_ASH/C_SO2 del import de dashboard.style, y ZOOM_VOLCAN del import de src.fetch.rammb_slider (dejar ZOOM_ZONE y fetch_frame_for_bounds).

- **[low]** `dashboard/views/modo_evento.py:17, 25` — Imports muertos en modo_evento.py (timedelta, HotSpot)
  - pyflakes confirma: timedelta (l.17) sin uso; HotSpot (l.25) sin uso (los hot spots se reciben como objetos pero nunca se anota/instancia el tipo).
  - **Fix:** Quitar timedelta del import de datetime y HotSpot del import de src.fetch.goes_fdcf (dejar fetch_latest_hotspots).

- **[low]** `dashboard/views/backfill_viewer.py:18, 21` — Imports muertos en backfill_viewer.py (json, typing.Optional)
  - pyflakes confirma: `import json` (l.18) sin uso (la decodificación se hace con Response.json()); `from typing import Optional` (l.21) sin uso (las anotaciones usan X | None).
  - **Fix:** Eliminar `import json` y `from typing import Optional`.

- **[low]** `dashboard/views/comparador.py:435-442` — _label() en modo baseline ignora su parámetro fallback_target
  - La función anidada _label(ts, fallback_target) nunca usa fallback_target: si ts is None retorna '-', si no formatea ts. Se invoca con un segundo argumento sin efecto. Sugiere una intención no implementada (probablemente mostrar el target cuando ts es None).
  - **Fix:** Usar el fallback (`return _fmt(fallback_target) if ts is None else _fmt(ts)`) si esa era la intención, o eliminar el parámetro y los argumentos en las dos llamadas.

- **[low]** `dashboard/views/modo_guardia.py:148` — Parámetro 'product' sin uso en _render_chile_with_hotspots
  - _render_chile_with_hotspots(frame, hotspots, volcan_name, product, show_rings=False) declara `product` pero nunca lo referencia en el cuerpo (la imagen viene de frame['image']). Se pasa desde ambos call sites. Inofensivo pero engañoso: un lector asume que el producto afecta el render (legend/colormap) cuando no.
  - **Fix:** Quitar el parámetro product de la firma y ambos call sites, o agregar un comentario de que se retiene por simetría.

- **[low]** `dashboard/views/volcat_viewer.py:92-93` — Arrays lat_arr/lon_arr sin uso en _fig_ssec_image
  - Las líneas 92-93 construyen `lat_arr = np.array([lat_min, lat_max])` y `lon_arr = np.array([lon_min, lon_max])` pero ninguno se usa después (el dominio del eje lo fijan el scatter invisible y add_layout_image). Allocaciones muertas.
  - **Fix:** Eliminar las líneas 92-93.

- **[low]** `scripts/build_backfill.py:133-144` — Loop de progreso RAMMB con enumerate(as_completed) sin uso y comentarios fantasma
  - En _build_rammb, `for i, fut in enumerate(as_completed(futures))` usa enumerate pero `i` nunca se usa. Hay dos comentarios que admiten que el tracking del producto por-future quedó abandonado y se reconstruye escaneando el disco después (rebuild por glob, que sí funciona). Código fantasma que confunde, no es bug funcional.
  - **Fix:** Reemplazar por `for fut in as_completed(futures)` y borrar los comentarios fantasma. Mantener el rebuild por glob.

- **[low]** `src/fetch/realearth_api.py:12, 15` — Imports muertos en realearth_api.py (requests, datetime) y volcat_api.py (requests)
  - realearth_api.py importa `requests` (l.15) pero todas las llamadas pasan por _get_session(); también `from datetime import datetime` (l.12) sin uso. volcat_api.py importa `requests` (l.16) sin uso directo tras migrar a la session compartida. Imports muertos que un linter marcaría.
  - **Fix:** Eliminar `import requests` y `from datetime import datetime` de realearth_api.py, y `import requests` de volcat_api.py, tras verificar con grep.


## 📄 Docs desactualizados (5)

- **[medium]** `scripts/generate_lascar_report.py:8, 100-101, 190` — Reporte dice 'hot spots últimas 24h' pero sólo consulta 1 hora
  - fetch_recent_hotspots llama `fetch_latest_hotspots(bounds=bounds, hours_back=1)` — sólo el scan más reciente / última hora. Pero la docstring del módulo (línea 8) lista 'Tabla hot spots ultimas 24h' y el placeholder vacío dice 'Sin hotspots\nen 24h' (línea 190). El lector operacional cree ver 24h de detecciones cuando ve ~1h (1-2 scans). Para una explosiva como Láscar, 1h puede no capturar el pulso térmico. Inconsistencia entre lo computado y lo rotulado.
  - **Fix:** Decidir la ventana y hacerla consistente: subir a hours_back=24 (más fiel a un reporte diario, a costa de más lecturas S3) o corregir docstring y label a '1h'. Recomendado: 24h con muestreo.

- **[low]** `src/fetch/wind_data.py:36, 50` — Docstrings de fetch_wind_point dan ejemplos '500hpa' (minúscula) que el API Open-Meteo rechaza con 400
  - El comentario advierte que Open-Meteo cambió a 'wind_speed_<N>hPa' (H mayúscula) y que el formato viejo da HTTP 400. WIND_LEVELS y DEFAULT_LEVEL usan 'hPa' correctamente. PERO los docstrings de fetch_wind_point dan ejemplos en minúscula ('500hpa', '850hpa', '300hpa') que, copiados literal como argumento level, construyen `wind_speed_500hpa` y disparan el 400 que el propio comentario advierte. La doc contradice al código.
  - **Fix:** Corregir los ejemplos del docstring a '500hPa','850hPa','300hPa' (o referir a las claves de WIND_LEVELS).

- **[low]** `src/process/ash_rgb.py:90-91` — Comentario del canal verde Ash/SO2 RGB dice 'invertido' pero la fórmula no invierte
  - El comentario dice 'Green: BT(8.4) - BT(11.2) → SO2 absorption (SO2 = muy negativo → invertido)', sugiriendo inversión de signo, pero la línea 91 calcula `green_data = bt11.values - bt14.values` = BT(8.4)-BT(11.2) SIN invertir, y normalize() con range (-4.0, 5.0) tampoco invierte. La imagen es consistente con el header del módulo y la config; lo erróneo es la palabra 'invertido' del comentario, que puede inducir a un futuro editor a 'arreglar' el signo y romper el producto.
  - **Fix:** Aclarar el comentario para que describa el mapeo aplicado (sin 'invertido') y citar la config como única fuente de verdad. Confirmar contra la guía EUMETSAT si SO2 debe salir oscuro o brillante en verde.

- **[low]** `src/config.py:121-128` — Comentario 'Ash/SO2 RGB (8.5-11-12)' nombra 8.5um pero la banda usada es 8.4um (B11)
  - El header de ASH_SO2_RGB y las leyendas (style.py:755) rotulan la receta '8.5 - 11 - 12 um', pero la banda ABI usada es B11 = 8.4 um (el cálculo usa B11). Es etiquetado heredado del nombre genérico EUMETSAT (a veces rotulado '8.5'), no un error de cálculo. Inconsistencia menor de cara al usuario.
  - **Fix:** Unificar el rótulo a 8.4 um en el comentario de config.py y en las leyendas de style.py, o aclarar que '8.5' es el nombre nominal de la receta EUMETSAT.

- **[low]** `dashboard/app.py:119` — BUILD_SHA hardcodeado y desactualizado respecto al HEAD del repo
  - BUILD_SHA = 'build-2026-06-10-backfill-villarrica' es un marcador manual para verificar que Streamlit Cloud sirve la versión actual. Los commits recientes son del 2026-06-12+ sobre Modo Sala/VOLCAT, así que el marcador ya no refleja el HEAD. Como su propósito es detectar versiones viejas cacheadas, un valor estancado anula su utilidad: el operador no puede distinguir app dormant de app actualizada.
  - **Fix:** Derivar el marcador automáticamente del git SHA en build/CI (variable de entorno o leer .git/HEAD al arrancar) en lugar de un string editado a mano.


## ✨ Polish (3)

- **[low]** `dashboard/views/heatmap_actividad.py:63-65` — zmax=12 satura el heatmap para eruptivos sostenidos (todo el día en el mismo rojo)
  - El heatmap diario fija zmax=12 scans. Con cadencia ~6 scans/hora, 12 scans = solo 2h de detección. Un volcán con actividad efusiva sostenida (Villarrica, Láscar) puede acumular >100 scans/día; todos saturan al mismo rojo, perdiendo poder discriminativo justo en los días más relevantes. El hovertemplate muestra el conteo exacto, pero el color (lo que se barre de un golpe de vista) no distingue 12 de 144.
  - **Fix:** Subir zmax a un valor más representativo de un día activo (p.ej. 48-72) o usar escala logarítmica/percentil sobre los datos reales. Si 12 es umbral deliberado de 'muy activo', documentarlo en el docstring.

- **[low]** `dashboard/views/zonas_fullscreen.py:606-612` — Anillo de 50 km dibujado fuera del panel zoom (radio del recorte ~38 km) — arcos parciales en las esquinas
  - _compose_volcan_panels dibuja RING_RADII_KM = [5,10,25,50] sobre un panel con radius_deg=0.35 → ~38.8 km de semilado. El anillo de 50 km cae fuera de los bordes del tile: PIL recorta el arco y se ven solo fragmentos en las esquinas; su etiqueta '50 km' queda en y negativa fuera del tile. No es incorrecto físicamente pero produce arcos parciales confusos en las 4 esquinas de cada panel.
  - **Fix:** Filtrar los anillos que caben dentro del panel: `for r_km in RING_RADII_KM: if r_km/km_per_px <= min(cx, ph/2): ...`, así solo se dibujan 5/10/25 km (y 50 si el recorte lo permite) sin arcos cortados.

- **[low]** `dashboard/views/rammb_viewer.py:722` — Typo visible en el info_panel del visor RAMMB ('Eleginado' → 'Eligiendo')
  - El panel informativo inicial dice 'Eleginado <b>Cobertura</b> podes ver todo Chile...'. 'Eleginado' no es una palabra; debería ser 'Eligiendo'. Texto de cara al usuario (geólogo OVDAS), visible cada vez que se abre la página sin animación cargada.
  - **Fix:** Reemplazar 'Eleginado' por 'Eligiendo' en la cadena HTML del info_panel.


## 🧪 Tests (9)

- **[high]** `tests/test_smoke.py:n/a (módulo no testeado)` — Planck inverso (rad_to_bt) sin test de round-trip ni manejo de radiancia ≤0
  - src/process/brightness_temp.py:rad_to_bt es el núcleo cuantitativo de todo el pipeline térmico (Ash RGB, BTD, SO2 dependen de BT en K) y no tiene ningún test. El CLAUDE.md lo lista en 'Comandos comunes' como 'Planck round-trip' pero ese test NO existe. Verificado: forward T=270K → rad=164.575 → rad_to_bt devuelve 270.0 exacto, y rad≤0 devuelve NaN. Sin test, una inversión de fk1/fk2 o de la corrección de banda (bc1/bc2) pasaría silenciosa y desplazaría todas las temperaturas, rompiendo los thresholds BTD/Ash sin error visible.
  - **Fix:** Agregar test_planck_roundtrip: construir Rad sintética hacia adelante desde T=270K (fk1=2e4,fk2=1.3e3,bc1=0.5,bc2=0.9995) y assertear np.isclose(rad_to_bt(...), 270.0, atol=1e-6); Rad con [-1.0, 0.0] → NaN; bt.attrs['units']=='K'.

- **[high]** `tests/test_smoke.py:n/a (módulo no testeado)` — BTD split-window y detección de ceniza (ash_detection) sin ningún test de threshold
  - src/process/ash_detection.py implementa la física central de detección de ceniza (Prata 1989): compute_btd_split_window, detect_ash_basic, detect_ash_enhanced, compute_ash_confidence. Ninguna tiene test. Son funciones puras sobre xr.DataArray, triviales de testear sin red, y codifican constantes científicas (BTD_ASH_THRESHOLD=-1.0, BTD_MIN_TEMP=200, BTD_TRI_THRESHOLD=0.0). Un cambio de signo (bt14-bt15 vs bt15-bt14) o un umbral mal aplicado invertiría la detección (ceniza↔nube) sin que nada lo atrape. compute_ash_confidence usa .where() encadenados cuyo orden (niveles 1→2→3) es sutil y propenso a romperse en refactors.
  - **Fix:** Agregar tests con DataArrays mínimos: detect_ash_basic (BTD<-1 y temp>200 → True; BTD>-1 → False; frío → False por filtro temp), detect_ash_enhanced (pasa BTD pero no tri-espectral → False), compute_ash_confidence (4 píxeles que den 0/1/2/3 exactos), compute_btd_split_window (btd == bt14-bt15 y attrs['ash_threshold']==-1.0).

- **[medium]** `tests/test_geo.py:38-92` — La reproyección del path FRP/hotspots (_abi_to_latlon, pyproj GEOS) no tiene test de consistencia con get_lat_lon
  - Existen dos implementaciones independientes de ABI→lat/lon: la analítica get_lat_lon (testeada) y la de pyproj GEOS _abi_to_latlon (goes_fdcf.py:144, usada por TODOS los hotspots y por frp_timeline.fetch_scan_sliced). Solo la primera tiene test. Si divergen (sweep mode, elipsoide GRS80 vs NetCDF, sat_lon -75.0 vs histórico -75.2), los diamantes de hotspots y la atribución FRP por volcán caerían en coordenadas equivocadas — el bug de offset ~17 km que el CLAUDE.md dice haber arreglado, sin red de regresión.
  - **Fix:** Agregar (con pytest.importorskip('pyproj')): test_abi_reproj_subsatellite (_abi_to_latlon(0,0,sat_lon=-75) → lat≈0, lon≈-75) y test_abi_consistency (para un punto sobre Chile, get_lat_lon y _abi_to_latlon coinciden dentro de ~0.05°).

- **[medium]** `tests/test_smoke.py:n/a (función no testeada)` — _parse_scan_time (DOY del nombre FDCF) sin test: off-by-one y año bisiesto sin red
  - goes_fdcf.py:87 _parse_scan_time extrae el datetime del nombre S3 usando day-of-year (base 1-ene + doy-1). Es la fuente del timestamp de TODOS los hotspots y de cada scan del FRP timeline. El cálculo doy-1 es clásicamente propenso a off-by-one, y al cruzar fin de año o un 29-feb un error desplazaría todos los scans un día. No hay test. parse_rammb_ts y _parse_volcat_frame_dt tampoco tienen cobertura.
  - **Fix:** Agregar test_parse_scan_time: '...s20261151200216...' → 2026-04-25 12:00:21 UTC (doy 115); doy=060 de 2024 → 2024-02-29; doy=001 → 1-ene; inválido → None. Y test_parse_rammb_ts: '20260425143055' → datetime(2026,4,25,14,30,55,utc) con tzinfo UTC.

- **[medium]** `tests/test_smoke.py:87-118` — sum_frp_per_volcano: el test no cubre el borde del radio, el cos(lat) ni el doble conteo multi-volcán
  - test_frp_sum_per_volcano prueba el caso feliz pero deja sin cubrir comportamientos documentados y frágiles: (1) la frontera exacta del radio (no se prueba un punto justo en radius_km que valide el '<='); (2) el doble conteo intencional (un hotspot dentro del radio de dos volcanes cuenta para ambos); (3) la conversión km usa cos(lat) en lon — sin un test de un par lon-only vs lat-only, una regresión que olvide cos(lat) pasaría.
  - **Fix:** Extender: hotspot a d==radius_km exacto → cuenta; a +epsilon → no; dos volcanes a <50km con hotspot equidistante → cuenta para ambos; un punto a +0.5° en lon (~43km a -39°, dentro) vs +0.5° en lat (~55km, fuera) → verifica que cos(lat) se aplica solo al lon.

- **[medium]** `tests/test_smoke.py:178-194` — _uniform_aspect_bounds / _volcat_uniform_views: la homogeneización de aspecto no verifica invariantes (aspect igual, centro, no-recorte)
  - _uniform_aspect_bounds (zonas_fullscreen.py:1097) y _volcat_uniform_views (l.265) ensanchan el lon de cada zona para igualar el aspect visual de las 4 — feature central del Modo Sala reescrito. El contrato es: (a) las 4 zonas terminan con mismo aspect (lon_span*cos_lat/lat_span), (b) el centro lon se preserva, (c) nunca recortan (lon_span_new >= original). Un bug deforma o recorta volcanes en el TV sin error. No hay test; el cache module-level (_UNIFORM_BOUNDS_CACHE) puede enmascarar cambios si no se resetea.
  - **Fix:** Agregar test que resetee _UNIFORM_BOUNDS_CACHE=None, llame _uniform_aspect_bounds() y assertee: aspect igual entre las 4 (atol 1e-6), lat_min/lat_max sin cambio vs VOLCANIC_ZONES, centro lon conservado, lon_span_new >= original. Repetir para _volcat_uniform_views vs VOLCAT_VIEW_4.

- **[medium]** `tests/test_smoke.py:n/a (función no testeada)` — _volcat_colorbar_strip: el recorte del colorbar (fracciones hardcodeadas) no tiene test de geometría
  - volcat_viewer.py:259 _volcat_colorbar_strip recorta una franja del pie de la imagen VOLCAT con fracciones quemadas (0.16..0.875 en x, 0.915..0.997 en y) sobre el layout SSEC 1000x821. Feature reciente de alto riesgo (commit a46b953). Si SSEC cambia el layout o las fracciones se editan mal (y_min>y_max), el crop se invierte/colapsa y el colorbar queda vacío, pero la función traga la excepción (return b'') en silencio. No hay test de que el recorte produzca una tira plausible.
  - **Fix:** Agregar test que monkeypatchee _volcat_image_bytes para devolver un PNG sintético 1000x821, llame _volcat_colorbar_strip y assertee: bytes no vacíos, aspect ancho (w/h > 4), altura ≈ (0.997-0.915)*821 ≈ 67px ±2.

- **[low]** `tests/test_smoke.py:121-138` — daily_rollup: no se prueba que la métrica es 'scans con detección' y no 'suma de hotspots' (bug histórico)
  - test_frp_daily_rollup verifica el conteo de scans pero no distingue del caso donde un solo scan tiene n=5: la métrica debe ser 1 (un scan), no 5. El CLAUDE.md documenta que el bug histórico era confundir suma de hotspots con persistencia ('conteo de 1 solo scan'). Sin ese caso, una regresión que sume los n en vez de contar scans pasaría el test actual.
  - **Fix:** Agregar un scan con n alto en un día aislado: scans=[{'t':'2026-06-09T10:00:00Z','n':{'Lascar':5}}] → assert roll['2026-06-09']['Lascar']==1 (un scan, NO 5). Fija el contrato 'persistencia = nº de scans con ≥1 detección'.

- **[low]** `tests/test_smoke.py:178` — _compose_4_zonas_png (producto principal del Modo Sala TV) sin smoke test, y _compose_volcan_panels sin test de anillos circulares
  - test_compose_volcan_panels solo verifica modo RGB y aspect 1.8-2.6. No cubre: (a) que pw=ph*cos_lat hace los anillos circulares (invariante documentada); (b) el compuesto de 4 zonas _compose_4_zonas_png, producto principal del Modo Sala TV reescrito, que no tiene ningún test ni smoke. Un error en km_per_px o en la proyección _xy desalinearía hotspots/triángulos respecto a la imagen.
  - **Fix:** Agregar test_compose_panel_circular_rings (verificar pw==round(ph*cos(radians(v.lat)))). Para _compose_4_zonas_png, un smoke test que monkeypatchee _zone_frame_cached y _hotspots_zone con datos sintéticos y assertee bytes PNG no vacíos abribles por PIL con altura ≈ ph+label_h.
